---
phase: 04-palette-panel
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - editor.html
autonomous: true
requirements:
  - PAL-01

must_haves:
  truths:
    - "色卡限制面板在左栏滚动区可见，包含：生成色卡、上传色卡、保存/加载、应用色卡按钮"
    - "点击色卡色块立即将该颜色同步到常驻调色盘（前景色改变，颜色选择器更新）"
    - "打开「色卡限制」toggle 时参数区自动展开"
  artifacts:
    - path: "editor.html"
      provides: "色卡面板 HTML 结构、CSS 样式、核心 JS（setCurrentPalette / renderSwatches / PAL-01 点击处理）"
      contains: "id=\"swatchesGrid\""
  key_links:
    - from: "swatch click handler"
      to: "syncColorUI()"
      via: "EditorState.foregroundColor = [r,g,b,255]; syncColorUI();"
      pattern: "EditorState\\.foregroundColor.*syncColorUI"
---

<objective>
将 web_ui.html 的色卡限制面板（HTML + CSS + JS）完整移植到 editor.html 左侧滚动区，并实现 PAL-01：点击色块自动同步到常驻调色盘。

Purpose: 让用户在 editor.html 中管理色卡并一键将色块颜色推入绘图前景色。
Output: editor.html 左栏出现完整的色卡限制面板；swatch 点击调用 syncColorUI()。
</objective>

<execution_context>
@/Users/calling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/calling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/calling/perfectPixel_ver1.1/.planning/PROJECT.md
@/Users/calling/perfectPixel_ver1.1/.planning/ROADMAP.md
@/Users/calling/perfectPixel_ver1.1/.planning/phases/04-palette-panel/04-CONTEXT.md
@/Users/calling/perfectPixel_ver1.1/.planning/phases/04-palette-panel/04-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from editor.html. -->

From editor.html (existing, DO NOT duplicate):
```javascript
// syncColorUI() — lines 477-525
// Updates all color picker UI elements from EditorState.foregroundColor
// MUST call highlightMatchingSwatches() at tail (added in Plan 02)
function syncColorUI() { /* _syncLock guard, updates hex/rgb inputs, redraws picker */ }

// EditorState (line 529)
const EditorState = {
  foregroundColor: [0, 0, 0, 255],  // [r,g,b,a] — mutated by PAL-01
  palette: [],                        // [[r,g,b], ...] — mirror of currentPalette
  pixels: null,                       // Uint8ClampedArray
  width: 0, height: 0,
};

// Existing color utilities (DO NOT re-add):
function hslToRgb(h, s, l) { ... }  // line 378
function rgbToHsl(r, g, b) { ... }  // line 389

// NOT in editor.html yet — must port from web_ui.html:
// rgbToHex(r,g,b) → '3-char or 6-char hex string' (web_ui.html line 585)
// hexToRgb(hex)   → [r,g,b] (web_ui.html line 588)
```

From editor.html (HTML structure):
```html
<!-- Mount point for palette panel (line 292-299) — REPLACE the placeholder div content -->
<div id="left-scroll">
  <div class="panel-card">   <!-- ← replace this entire div's content -->
    <div class="panel-header" ...>
      <span>色卡限制</span>
      <span>[Phase 4]</span>  <!-- ← will be replaced by the real panel -->
    </div>
  </div>
</div>
```

From editor.html CSS (line 111-122):
```css
#canvas-area { display: flex; flex-direction: row; overflow: auto; }
/* NOTE: #canvas-area is flex-row — palette-result-panel goes here as flex sibling (Plan 02) */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移植色卡面板 HTML + CSS</name>
  <files>editor.html</files>
  <action>
读取 web_ui.html 的色卡面板 HTML（lines 354-488）和相关 CSS（lines 108-230）。

**HTML 移植：**
将 `#left-scroll` 内的占位符 `.panel-card` 整体替换为从 web_ui.html 搬来的完整色卡面板 HTML。结构保持一致：
- 最外层用 `<div class="palette-section" id="paletteSection">` 包裹（而非 `.panel-card`），与 web_ui.html 保持 ID 一致
- 内部 `<div class="palette-header" id="paletteHeader">` 含 toggle checkbox、标题、chevron
- `<div class="palette-body hidden" id="paletteBody">` 含全部参数区（生成、上传、色块展示区、保存/加载、导出格式、映射模式、应用按钮）

**ID 冲突检查（在移植前 grep 确认）：**
- `paletteHeader`, `paletteBody`, `paletteEnabled`, `swatchesGrid`, `swatchCount`, `generateBtn`, `applyPaletteBtn`, `paletteName`, `savePaletteBtn`, `savedPalettesDropdown`, `savedPaletteTrigger`, `savedPaletteTriggerText`, `savedPaletteOptions`, `loadPaletteBtn`, `deletePaletteBtn`
- 以上 ID 在 editor.html 中均不存在（grep 可确认）→ 直接移植，无需改名

**Color Editor Popup HTML：**
将 web_ui.html lines 542-558 的 `<div class="color-popup" id="colorPopup">` 移植到 editor.html `#layout` div 之外（紧接在 `</div><!-- /#layout -->` 之后、`<script>` 之前），`position: fixed; z-index: 999`（确保高于 canvas 层 z-index 1/2/3）。

**CSS 移植：**
将 web_ui.html lines 108-230 的 palette CSS 块（`.palette-section`, `.palette-header`, `.palette-body`, `.toggle-switch`, `.tabs`, `.tab-btn`, `.custom-file-upload`, `.swatches-label`, `.swatches-count`, `.swatches-grid`, `.swatch`, `.swatch:hover`, `.swatch .del-btn`, `.swatches-empty`, `.custom-select`, `.color-popup` 等）复制到 editor.html `<style>` 末尾。

**Swatch glow 溢出修复（PAL-02 预置，RESEARCH 明确要求）：**
修改 `.swatches-grid` CSS：移除 `overflow: hidden/auto`；在外层包裹一个 `.swatches-outer { max-height: 140px; overflow-y: auto; }` div，`.swatches-grid { overflow: visible; }` 使 box-shadow 不被裁剪。
  </action>
  <verify>
在浏览器打开 http://localhost:5010/editor（确保 Flask 已运行），左栏出现「色卡限制」面板，点击 chevron 可展开/折叠，参数区 UI 可见（包含生成色卡按钮、上传区、色块区、保存/加载、应用色卡按钮）。JS 控制台无报错。
  </verify>
  <done>editor.html 左栏显示完整色卡限制面板 HTML 结构；所有相关 CSS 类在 editor.html 的 style 块中定义；Color Editor Popup 挂载在 #layout 外；browser console 无 "Cannot read" 等 ID 引用错误。</done>
</task>

<task type="auto">
  <name>Task 2: 移植色卡 JS（核心函数 + PAL-01 swatch 点击同步）</name>
  <files>editor.html</files>
  <action>
**1. 添加缺失的颜色工具函数（在 hslToRgb/rgbToHsl 附近，script 顶部）：**
```javascript
function rgbToHex(r, g, b) {
  return [r, g, b].map(v => v.toString(16).padStart(2, '0')).join('');
}
function hexToRgb(hex) {
  hex = hex.replace('#', '');
  if (hex.length === 3) hex = hex.split('').map(c => c + c).join('');
  const n = parseInt(hex, 16);
  return [(n >> 16) & 255, (n >> 8) & 255, n & 255];
}
```

**2. 移植 Palette Panel 状态变量（在 EditorState 声明之后）：**
```javascript
// ── Palette Panel State ───────────────────────────────────────────────
let currentPalette     = [];   // [[r,g,b], ...]
let editingSwatchIdx   = null;
let selectedPaletteKey = '';
let currentPaletteMeta = { name: '', source: 'upload', algorithm: '', count: 0 };
```

**3. 从 web_ui.html 移植以下函数（lines 846-1139），每处改动说明如下：**

`setCurrentPalette(palette, meta)`:
- 移植原函数
- 末尾额外添加：`EditorState.palette = currentPalette.slice();`（同步到 EditorState）

`renderSwatches()`:
- **PAL-01 修改**：将 web_ui.html 中的 `sw.addEventListener('click', () => openColorEditor(idx, sw));` 替换为：
```javascript
sw.addEventListener('click', () => {
  const [r, g, b] = currentPalette[idx];
  EditorState.foregroundColor = [r, g, b, 255];
  syncColorUI();  // 触发 PAL-02 高亮（Plan 02 添加 tail hook 后生效）
});
sw.addEventListener('dblclick', e => { e.stopPropagation(); openColorEditor(idx, sw); });
```
- 末尾调用 `highlightMatchingSwatches()`（此函数在 Plan 02 添加，此处需要前向引用 —— 用 `if (typeof highlightMatchingSwatches === 'function') highlightMatchingSwatches();` 防止 Plan 01 单独运行时报错）

`deleteSwatch(idx)`: 移植原函数，无修改

`openColorEditor(idx, swEl)` / `closeColorEditor()`: 移植原函数，确保 ID 前缀（color-popup 用 `colorPopup`，内部 inputs 使用 `popup-r`, `popup-g`, `popup-b`, `popup-hex` —— 若 web_ui.html 原 ID 与 editor.html 已有 ID 冲突则加 `pal-` 前缀）

Save/load 相关函数（`savePalette`, `loadPalette`, `deleteSavedPalette`, `populateSavedPalettes` 等，lines 960-1056）: 移植原函数，localStorage key 保持 `pp_saved_palettes`

Export palette 函数（lines 1058-1080）: 移植原函数

**4. 移植 DOMContentLoaded 事件绑定（色卡面板部分）：**
在 editor.html 的 DOMContentLoaded 回调中添加（不覆盖已有绑定，追加到末尾）：
- `paletteHeader` click → toggle `paletteBody.hidden`，更新 chevron
- `paletteEnabled` change → **自动展开**（CONTEXT.md 锁定决策）：toggle ON 时 `paletteBody.classList.remove('hidden'); paletteHeader.classList.add('open');`；toggle OFF 不强制折叠（仅更新按钮 disabled 状态）
- `applyPaletteBtn` disabled gate：`!paletteEnabled.checked || !EditorState.pixels || currentPalette.length === 0`（注意：无 `currentPixelArtB64`，以 `EditorState.pixels` 代替）
- `generateBtn`, `savePaletteBtn`, `loadPaletteBtn`, `deletePaletteBtn`, 文件上传, 导出按钮 等全部绑定，与 web_ui.html 保持一致
- `applyPaletteBtn` click 暂绑定 `applyPalette` stub（Plan 02 实现完整版）：`document.getElementById('applyPaletteBtn').addEventListener('click', () => { /* Plan 02 */ });`
  </action>
  <verify>
1. 打开 http://localhost:5010/editor，Console 无 JS 错误
2. 在 browser console 执行 `currentPalette` → 返回 `[]`（变量存在）
3. 点击「生成色卡」按钮（在输入色卡数量后）→ 发送请求（Network tab 可见 POST /api/generate-palette）
4. 点击一个色块 → browser console 执行 `EditorState.foregroundColor` → 返回该色块的 RGBA 值
5. 点击色块后调色盘颜色选择器（色轮）更新为对应颜色
  </verify>
  <done>currentPalette 变量可访问；renderSwatches() 函数存在；点击 swatch 触发 EditorState.foregroundColor 更新并调用 syncColorUI()；palette JS 核心函数全部可调用；toggle ON 自动展开参数区。</done>
</task>

</tasks>

<verification>
验证 Plan 01 完整性：
- editor.html 左栏出现「色卡限制」面板，含所有控件
- 点击 toggle ON → 参数区自动展开
- 点击任意色块 → EditorState.foregroundColor 更新，调色盘颜色更新
- browser console 无 JS 错误
</verification>

<success_criteria>
- PAL-01 实现：点击 swatch → syncColorUI() 调用 → 调色盘颜色立即更新
- 面板功能完整：生成色卡、上传 .act/.gpl/.pal/.png、保存/加载到 localStorage、导出格式、映射模式选择可见
- 工具状态不变：点击 swatch 不切换 activeTool
- CSS glow 溢出已修复（swatches-outer + overflow:visible 结构就绪）
</success_criteria>

<output>
完成后创建 `.planning/phases/04-palette-panel/04-01-SUMMARY.md`
</output>
