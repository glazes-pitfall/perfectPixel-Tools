---
phase: 05.1-selection-visual-polish-dpr-fix-inverse-color-preview-slow-purple-gray-ants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - editor.html
autonomous: false
requirements:
  - VISUAL-FIX-01
must_haves:
  truths:
    - "选区蚂蚁线在任意背景（黑/白/彩色像素）上均清晰可见——黑底显白边，白底显黑边"
    - "蚂蚁线线宽为 1 逻辑像素（DPR=2 Retina 屏上为 2 物理像素），不再出现 4px 宽粗线"
    - "Marquee 拖拽预览虚线框与蚂蚁线视觉一致——同为 1px 细线，反色渲染"
    - "紫灰色视觉症状消失（该症状为 DPR 双重应用的结果，随根因修复自动消除）"
    - "rebuildAntsPath / scheduleAnts / RAF 循环结构未被改动"
  artifacts:
    - path: "editor.html"
      provides: "修复后的 drawAnts() 和 _marqueeDrawPreview() 函数"
      contains: "globalCompositeOperation = 'difference'"
  key_links:
    - from: "drawAnts()"
      to: "selCtx (selection-canvas context)"
      via: "globalCompositeOperation='difference' 单次白色描边"
      pattern: "globalCompositeOperation.*difference"
    - from: "_marqueeDrawPreview()"
      to: "selCtx (selection-canvas context)"
      via: "globalCompositeOperation='difference' + strokeRect"
      pattern: "globalCompositeOperation.*difference"
---

<objective>
修复 editor.html 中选区渲染的两处 DPR 双重应用 Bug，并切换为反色合成描边。

Purpose: selCtx 在 initCanvases() 中已永久 scale(dpr, dpr)，drawAnts() 和 _marqueeDrawPreview() 再次包裹 save/scale/restore 导致路径以 DPR²=4 倍渲染（Retina 上 lineWidth=1 实际渲染为 4px）。双色描边（白+黑）在 4px 宽度下叠合产生紫灰色混色，即用户看到的"慢速紫灰色蚂蚁"症状。

Output: 修复后的 editor.html，drawAnts() 和 _marqueeDrawPreview() 使用逻辑坐标 clearRect + 无 save/scale/restore + globalCompositeOperation='difference' 单白色描边。
</objective>

<execution_context>
@/Users/calling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/calling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-selection-visual-polish-dpr-fix-inverse-color-preview-slow-purple-gray-ants/05.1-CONTEXT.md
@.planning/phases/05.1-selection-visual-polish-dpr-fix-inverse-color-preview-slow-purple-gray-ants/05.1-RESEARCH.md

<interfaces>
<!-- 关键上下文：selCtx 在 initCanvases() 中的永久 DPR 缩放 (editor.html lines 1338–1369) -->
<!-- 这是"永久应用一次"的正确 DPR 模式——draw 函数不得重复 scale -->

```javascript
// initCanvases() — 已永久应用 DPR（不要修改这里）
[selCanvas, cursorCanvas].forEach(canvas => {
  canvas.width  = width  * dpr;   // 物理缓冲区尺寸
  canvas.height = height * dpr;
  canvas.style.width  = width  + 'px';  // CSS 逻辑尺寸
  canvas.style.height = height + 'px';
});
selCtx    = selCanvas.getContext('2d');
selCtx.scale(dpr, dpr);    // 永久应用 — 之后所有 selCtx 绘制使用逻辑坐标
```

<!-- 当前有 Bug 的代码 (editor.html lines 1174–1191) -->
```javascript
// 有 Bug — drawAnts():
function drawAnts() {
  if (!EditorState.selectionMask || !_antsPath) { antsRafId = null; return; }
  const dpr = window.devicePixelRatio || 1;
  selCtx.clearRect(0, 0, selCanvas.width, selCanvas.height);  // BUG: 物理像素坐标
  selCtx.save();
  selCtx.scale(dpr, dpr);  // BUG: 双重 DPR
  selCtx.lineWidth = 1;
  selCtx.setLineDash([4, 4]);
  selCtx.strokeStyle = '#ffffff';
  selCtx.lineDashOffset = antsDashOffset;
  selCtx.stroke(_antsPath);
  selCtx.strokeStyle = '#000000';
  selCtx.lineDashOffset = antsDashOffset + 4;
  selCtx.stroke(_antsPath);
  selCtx.restore();
  antsDashOffset = (antsDashOffset - 1 + 8) % 8;
  antsRafId = requestAnimationFrame(drawAnts);
}

// 有 Bug — _marqueeDrawPreview() (lines 2492–2506):
function _marqueeDrawPreview(rx, ry, rw, rh) {
  const dpr = window.devicePixelRatio || 1;
  selCtx.clearRect(0, 0, selCanvas.width, selCanvas.height);  // BUG: 物理像素坐标
  selCtx.save();
  selCtx.scale(dpr, dpr);  // BUG: 双重 DPR
  selCtx.lineWidth = 1;
  selCtx.setLineDash([4, 4]);
  selCtx.strokeStyle = '#ffffff';
  selCtx.lineDashOffset = 0;
  selCtx.strokeRect(rx + 0.5, ry + 0.5, rw, rh);
  selCtx.strokeStyle = '#000000';
  selCtx.lineDashOffset = 4;
  selCtx.strokeRect(rx + 0.5, ry + 0.5, rw, rh);
  selCtx.restore();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 drawAnts() — 删除双重 DPR，切换反色合成</name>
  <files>editor.html</files>
  <action>
精确替换 editor.html 第 1174–1191 行的 drawAnts() 函数体。

**删除内容（第 1176–1188 行）：**
- `const dpr = window.devicePixelRatio || 1;`
- `selCtx.clearRect(0, 0, selCanvas.width, selCanvas.height);`（物理坐标版本）
- `selCtx.save();`
- `selCtx.scale(dpr, dpr);`（双重 DPR — 根本 Bug）
- 第二次 `selCtx.strokeStyle = '#000000';`
- 第二次 `selCtx.lineDashOffset = antsDashOffset + 4;`
- 第二次 `selCtx.stroke(_antsPath);`
- `selCtx.restore();`

**添加内容（替换为）：**
```javascript
function drawAnts() {
  if (!EditorState.selectionMask || !_antsPath) { antsRafId = null; return; }
  selCtx.clearRect(0, 0, EditorState.width, EditorState.height);
  selCtx.lineWidth = 1;
  selCtx.setLineDash([4, 4]);
  selCtx.globalCompositeOperation = 'difference';
  selCtx.strokeStyle = '#ffffff';
  selCtx.lineDashOffset = antsDashOffset;
  selCtx.stroke(_antsPath);
  selCtx.globalCompositeOperation = 'source-over';
  antsDashOffset = (antsDashOffset - 1 + 8) % 8;
  antsRafId = requestAnimationFrame(drawAnts);
}
```

**关键约束：**
- `rebuildAntsPath()`（第 1161–1172 行）不要触碰
- `scheduleAnts()`（第 1193–1197 行）不要触碰
- `initCanvases()` 中的 `selCtx.scale(dpr, dpr)` 不要触碰（这是正确的，只能存在一次）
- 用 `EditorState.width` / `EditorState.height`（逻辑像素），不用 `selCanvas.width` / `selCanvas.height`（物理像素）
  </action>
  <verify>
    <automated>grep -n "globalCompositeOperation.*difference" /Users/calling/perfectPixel_ver1.1/editor.html | head -5</automated>
  </verify>
  <done>
    - editor.html 中 drawAnts() 不包含 save/scale/restore 或第二次描边
    - clearRect 使用 EditorState.width/height
    - globalCompositeOperation='difference' 在 stroke 前设置，'source-over' 在 stroke 后立即重置
    - grep 输出显示至少两处 'difference' 出现（drawAnts + _marqueeDrawPreview 各一处，Task 2 完成后）
  </done>
</task>

<task type="auto">
  <name>Task 2: 修复 _marqueeDrawPreview() — 删除双重 DPR，切换反色合成</name>
  <files>editor.html</files>
  <action>
精确替换 editor.html 第 2492–2506 行的 _marqueeDrawPreview() 函数体。

**删除内容（第 2493–2505 行）：**
- `const dpr = window.devicePixelRatio || 1;`
- `selCtx.clearRect(0, 0, selCanvas.width, selCanvas.height);`（物理坐标版本）
- `selCtx.save();`
- `selCtx.scale(dpr, dpr);`（双重 DPR）
- 第二次 `selCtx.strokeStyle = '#000000';`
- 第二次 `selCtx.lineDashOffset = 4;`
- 第二次 `selCtx.strokeRect(rx + 0.5, ry + 0.5, rw, rh);`
- `selCtx.restore();`

**添加内容（替换为）：**
```javascript
function _marqueeDrawPreview(rx, ry, rw, rh) {
  selCtx.clearRect(0, 0, EditorState.width, EditorState.height);
  selCtx.lineWidth = 1;
  selCtx.setLineDash([4, 4]);
  selCtx.globalCompositeOperation = 'difference';
  selCtx.strokeStyle = '#ffffff';
  selCtx.lineDashOffset = 0;
  selCtx.strokeRect(rx + 0.5, ry + 0.5, rw, rh);
  selCtx.globalCompositeOperation = 'source-over';
}
```

**关键约束：**
- `strokeRect(rx + 0.5, ry + 0.5, rw, rh)` 中的 +0.5 偏移必须保留（防止亚像素模糊）
- Marquee 的 onDown/onMove/onUp 交互逻辑不要触碰（仅修改此辅助绘制函数内部）
- 这是同一个 editor.html 文件的第二次修改，在 Task 1 完成后执行
  </action>
  <verify>
    <automated>grep -n "globalCompositeOperation\|save\(\)\|scale(dpr\|selCanvas\.width\|selCanvas\.height" /Users/calling/perfectPixel_ver1.1/editor.html | grep -v "initCanvases\|cursorCtx\|canvas\.width\|canvas\.height\|style\." | head -20</automated>
  </verify>
  <done>
    - editor.html 中 _marqueeDrawPreview() 不包含 save/scale/restore 或第二次 strokeRect
    - clearRect 使用 EditorState.width/height
    - globalCompositeOperation='difference' 在 strokeRect 前设置，'source-over' 在之后立即重置
    - 验证命令输出中，drawAnts 和 _marqueeDrawPreview 区域不出现 selCanvas.width/height（initCanvases 区域除外）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 目视验证蚂蚁线视觉效果</name>
  <what-built>
    修复了 editor.html 中两处选区渲染 Bug：
    1. drawAnts() — 删除双重 DPR scale，clearRect 改为逻辑坐标，切换为 difference 合成单白色描边
    2. _marqueeDrawPreview() — 同上，针对 Marquee 拖拽预览虚线框
  </what-built>
  <how-to-verify>
    1. 启动 Flask：`python3 /Users/calling/perfectPixel_ver1.1/web_app.py`
    2. 打开编辑器：http://localhost:5010/editor
    3. 上传任意像素画图片（或用已有图片）
    4. 选择 Marquee 工具 (M)，在画布上拖拽绘制选区：
       - 拖拽过程中：虚线框应为细线（约 1px），不应出现粗紫灰色边框
       - 释放后：蚂蚁线应流畅动画，线条纤细
    5. 确认反色效果：
       - 在深色像素区域：蚂蚁线应显示为白色/亮色
       - 在浅色像素区域：蚂蚁线应显示为深色
       - 不再出现紫色或灰色混合
    6. 选择 Magic Wand 工具 (W)，点击一个色块验证蚂蚁线同样正常
    7. Cmd+D 取消选区，确认蚂蚁线消失干净
  </how-to-verify>
  <resume-signal>输入 "approved" 确认视觉效果正常，或描述仍存在的问题</resume-signal>
</task>

</tasks>

<verification>
修复后验证检查清单：
1. `grep -n "scale(dpr, dpr)" editor.html` — 只应在 initCanvases() 区域出现（selCtx + cursorCtx 各一次），drawAnts 和 _marqueeDrawPreview 区域不得出现
2. `grep -n "globalCompositeOperation.*difference" editor.html` — 应出现两次（drawAnts 和 _marqueeDrawPreview 各一次）
3. `grep -n "globalCompositeOperation.*source-over" editor.html` — 应出现两次（每次 difference 后必须重置）
4. `grep -n "selCanvas\.width\|selCanvas\.height" editor.html` — 在 clearRect 调用中不得出现（只能在 initCanvases 的 canvas.width= 赋值中出现）
5. 目视验证：蚂蚁线为 1px 细线，反色可见，无紫灰色
</verification>

<success_criteria>
- 蚂蚁线在所有背景颜色上均清晰可见（difference 合成保证）
- 蚂蚁线线宽为 1 逻辑像素（DPR=2 时 2 物理像素，不再是 4px）
- Marquee 拖拽预览虚线框与最终蚂蚁线视觉一致
- 紫灰色症状彻底消失
- rebuildAntsPath / scheduleAnts / initCanvases / 选区工具交互代码均未被改动
</success_criteria>

<output>
完成后创建 `.planning/phases/05.1-selection-visual-polish-dpr-fix-inverse-color-preview-slow-purple-gray-ants/05.1-01-SUMMARY.md`

SUMMARY 中记录：
- 修改的精确行号（修改前 vs 修改后）
- difference 合成对透明 selCanvas 背景的实际视觉表现（验证结果）
- lineWidth=1 在 DPR=2 下的实际渲染宽度（验证结果）
</output>
