---
phase: 04.1-phase-4
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - editor.html
autonomous: true
requirements:
  - PAL-03
  - PAL-04

must_haves:
  truths:
    - "含半透明像素的图片生成色卡时，alpha ≤ 127 的像素不产生任何色号条目（不再出现虚假黑色）"
    - "点击'应用色卡'后画布内容直接被替换（EditorState.pixels 更新，flushPixels 重绘）"
    - "alpha ≥ 128 的像素应用后替换为最近色卡色且 alpha=255；alpha ≤ 127 的像素变为纯透明 (0,0,0,0)"
    - "应用色卡操作通过 Cmd+Z 可完全撤销"
  artifacts:
    - path: "editor.html"
      provides: "PAL-03 alpha 过滤（generateBtn handler）+ PAL-04 纯 JS 破坏性 applyPalette"
      contains: "nearestPaletteColor"
  key_links:
    - from: "generateBtn click handler"
      to: "Flask /api/generate-palette"
      via: "filtered Uint8ClampedArray（alpha 已规范化）→ toDataURL"
      pattern: "filtered\\[i\\+3\\] = 255"
    - from: "applyPalette()"
      to: "EditorState.pixels"
      via: "直接遍历写入（无 fetch）+ pushHistory + flushPixels"
      pattern: "pushHistory.*flushPixels|nearestPaletteColor"
---

<objective>
修正 Phase 4 遗留的两个缺陷：

1. **PAL-03（Alpha 分类修正）**：`generateBtn` click handler 在将 `EditorState.pixels` 编码为 base64 发送给 Flask 前，对半透明像素未做规范化处理，导致 Flask 端（`cv2.IMREAD_COLOR`）将透明区域合成为黑色，黑色像素进入量化池，色卡中出现虚假黑色条目。修复方式：在 JS 侧写入 offscreen canvas 前构建 filtered 数组（alpha ≥ 128 → alpha 强制 255；alpha ≤ 127 → RGBA 全零）。

2. **PAL-04（破坏性应用色卡）**：现有 `applyPalette()` 是 async 函数，调用 Flask `/api/apply-palette`，仅更新预览图不写入 `EditorState.pixels`。替换为同步纯 JS 实现：`pushHistory()` → 遍历 `EditorState.pixels` 按 alpha 阈值替换 → `flushPixels()`。

Purpose: 修正色卡功能的语义正确性，使色卡应用成为真正可撤销的破坏性画布操作。
Output: `editor.html`（单文件修改，2 处改动）
</objective>

<execution_context>
@/Users/calling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/calling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-phase-4/04.1-CONTEXT.md
@.planning/phases/04.1-phase-4/04.1-RESEARCH.md

<interfaces>
<!-- 以下来自直接读取 editor.html，供 executor 直接使用，无需探索代码库 -->

<!-- pushHistory / flushPixels（源：editor.html 第 1197 行、第 1347 行）-->
```javascript
function pushHistory() {
  EditorState.history.splice(EditorState.historyIndex + 1);
  EditorState.history.push(EditorState.pixels.slice());
  if (EditorState.history.length > EditorState.MAX_HISTORY) {
    EditorState.history.shift();
  } else {
    EditorState.historyIndex++;
  }
  updateHistoryButtons();
}

function flushPixels() {
  pixelCtx.putImageData(
    new ImageData(EditorState.pixels, EditorState.width, EditorState.height),
    0, 0
  );
}
```

<!-- currentPalette 格式（源：editor.html 第 967 行）-->
```javascript
let currentPalette = [];  // [[r, g, b], ...] — 纯 RGB，无 alpha，已规范化为整数
```

<!-- 当前 generateBtn offscreen canvas 写入（源：editor.html ~第 2644 行，即将修改）-->
```javascript
// 问题代码（未过滤 alpha）：
const offCanvas = document.createElement('canvas');
offCanvas.width = EditorState.width; offCanvas.height = EditorState.height;
const offCtx = offCanvas.getContext('2d');
offCtx.putImageData(new ImageData(EditorState.pixels, EditorState.width, EditorState.height), 0, 0);
const b64 = offCanvas.toDataURL('image/png').split(',')[1];
```

<!-- 当前 applyPalette 范围（源：editor.html 第 1862–1904 行，即将整体替换）-->
<!-- async function，调用 fetch('/api/apply-palette')，不写 EditorState.pixels -->

<!-- 按钮禁用门控（源：editor.html 第 1638–1641 行，保留不动）-->
```javascript
const applyBtn = document.getElementById('applyPaletteBtn');
const palEnabled = document.getElementById('paletteEnabled');
if (applyBtn && palEnabled) {
  applyBtn.disabled = !palEnabled.checked || !EditorState.pixels || currentPalette.length === 0;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PAL-03 — 修正 generateBtn offscreen canvas alpha 过滤</name>
  <files>editor.html</files>
  <action>
在 editor.html 中定位 `generateBtn` click handler 内写入 offscreen canvas 的代码段（研究报告标注为约第 2644–2650 行）。将以下原始代码：

```javascript
offCtx.putImageData(new ImageData(EditorState.pixels, EditorState.width, EditorState.height), 0, 0);
```

替换为先构建 filtered 数组再 putImageData：

```javascript
// PAL-03: 写入前规范化 alpha — alpha >= 128 强制 255，alpha <= 127 清零（避免 Flask IMREAD_COLOR 合成出虚假黑色）
const src = EditorState.pixels;
const filtered = new Uint8ClampedArray(src.length);
for (let i = 0; i < src.length; i += 4) {
  const a = src[i + 3];
  if (a >= 128) {
    filtered[i]   = src[i];
    filtered[i+1] = src[i+1];
    filtered[i+2] = src[i+2];
    filtered[i+3] = 255;
  } else {
    filtered[i] = filtered[i+1] = filtered[i+2] = filtered[i+3] = 0;
  }
}
offCtx.putImageData(new ImageData(filtered, EditorState.width, EditorState.height), 0, 0);
```

只替换这一处 putImageData 调用，后续 `toDataURL` 及 `fd.append` 保持不变。不修改任何其他部分。
  </action>
  <verify>
```bash
grep -n "filtered" /Users/calling/perfectPixel_ver1.1/editor.html | head -20
# 应看到：filtered[i+3] = 255 以及 new ImageData(filtered, EditorState.width
```
  </verify>
  <done>editor.html 中 generateBtn handler 内 putImageData 调用使用 filtered 数组；alpha &lt; 128 的像素 RGBA 全为 0，alpha ≥ 128 的像素 alpha 强制 255</done>
</task>

<task type="auto">
  <name>Task 2: PAL-04 — 替换 applyPalette 为同步纯 JS 破坏性实现</name>
  <files>editor.html</files>
  <action>
在 editor.html 中定位 `async function applyPalette()` 整个函数体（研究报告标注为第 1862–1904 行）。执行以下操作：

**步骤 A**：在该函数定义的正上方，插入 `nearestPaletteColor` 辅助函数（若不存在）：

```javascript
// ── PAL-04: 最近色卡色匹配（RGB 欧氏距离平方，不开根号）──────────────────
function nearestPaletteColor(r, g, b, palette) {
  let bestIdx = 0, bestDist = Infinity;
  for (let i = 0; i < palette.length; i++) {
    const dr = r - palette[i][0], dg = g - palette[i][1], db = b - palette[i][2];
    const dist = dr*dr + dg*dg + db*db;
    if (dist < bestDist) { bestDist = dist; bestIdx = i; }
  }
  return palette[bestIdx];  // [r, g, b]
}
```

**步骤 B**：将整个 `async function applyPalette() { ... }` 函数体替换为以下同步实现：

```javascript
// ── PAL-04: 破坏性应用色卡 — 直接写入 EditorState.pixels + pushHistory ──
function applyPalette() {
  if (!EditorState.pixels || currentPalette.length === 0) return;
  pushHistory();  // save-before（瞬时操作，参照 paint bucket 模式）
  const px = EditorState.pixels;
  const pal = currentPalette;
  for (let i = 0; i < px.length; i += 4) {
    const a = px[i + 3];
    if (a <= 127) {
      // 透明一侧 → 纯透明
      px[i] = 0; px[i+1] = 0; px[i+2] = 0; px[i+3] = 0;
    } else {
      // 不透明一侧 → 最近色卡色，alpha 强制 255
      const [nr, ng, nb] = nearestPaletteColor(px[i], px[i+1], px[i+2], pal);
      px[i] = nr; px[i+1] = ng; px[i+2] = nb; px[i+3] = 255;
    }
  }
  flushPixels();
  palShowStatus('色卡已应用', 'success');
}
```

**关键约束（严格遵守）：**
- 函数从 `async` 改为同步（去掉 `async` 关键字），不调用任何 `fetch`
- `pushHistory()` 必须在遍历像素**之前**调用（save-before 模型）
- 直接修改 `EditorState.pixels`，不创建副本
- 不修改 `applyPaletteBtnEl.addEventListener('click', applyPalette)` 绑定（位于约第 2796 行，保留）
- 不修改 `applyBtn.disabled` 门控逻辑（位于约第 1638–1641 行和第 2564–2566 行，保留）
- 新函数是同步的，无需 `btn.disabled = true` 异步保护
  </action>
  <verify>
```bash
# 确认 async 已去掉
grep -n "async function applyPalette" /Users/calling/perfectPixel_ver1.1/editor.html
# 应无输出

# 确认新实现存在
grep -n "nearestPaletteColor\|pushHistory\|flushPixels" /Users/calling/perfectPixel_ver1.1/editor.html | grep -A2 -B2 "applyPalette\|nearestPalette"

# 确认 fetch('/api/apply-palette') 不再出现
grep -n "api/apply-palette" /Users/calling/perfectPixel_ver1.1/editor.html
# 应无输出
```
  </verify>
  <done>`applyPalette` 函数为同步实现，不调用 Flask API；函数体包含 pushHistory → 像素遍历（alpha 阈值 127）→ flushPixels；nearestPaletteColor 辅助函数存在于同文件中</done>
</task>

</tasks>

<verification>
Task 1 完成后：
- `grep -n "filtered\[i\+3\] = 255" editor.html` 有输出
- `grep -n "api/generate-palette" editor.html` 仍有输出（Flask 生成色卡端点保留）

Task 2 完成后：
- `grep -n "async function applyPalette" editor.html` 无输出
- `grep -n "api/apply-palette" editor.html` 无输出
- `grep -n "nearestPaletteColor" editor.html` 有输出（函数定义 + 调用两处）
- `grep -n "pushHistory" editor.html` 在 applyPalette 函数内仍有调用

整体验证（浏览器验证在 Plan 02 的 checkpoint 中执行）：
- 打开 Flask 后载入含透明/半透明像素的 PNG
- 生成色卡 → 色卡中无虚假黑色
- 应用色卡 → 画布直接更新，半透明像素变透明，有色像素替换为最近色卡色
- Cmd+Z 可完全撤销应用色卡操作
</verification>

<success_criteria>
- PAL-03：generateBtn 发送给 Flask 的像素数据中，alpha ≤ 127 的像素 RGBA 全为 0（不参与量化），alpha ≥ 128 的像素 alpha 强制为 255
- PAL-04：applyPalette 函数为同步实现；pushHistory 在像素写入前调用；EditorState.pixels 直接被修改；操作后 flushPixels 重绘画布
- 两处修改不破坏现有按钮绑定（applyPaletteBtnEl click 绑定保留）和禁用门控（applyBtn.disabled 逻辑保留）
</success_criteria>

<output>
完成后创建 `.planning/phases/04.1-phase-4/04.1-01-SUMMARY.md`，记录：
- 两处改动的实际行号范围
- nearestPaletteColor 函数所在行号
- applyPalette 新实现所在行号
- 任何与研究报告行号有偏差的情况
</output>
