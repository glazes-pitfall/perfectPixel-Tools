---
phase: 04.1-phase-4
plan: "02"
type: execute
wave: 2
depends_on:
  - "04.1-01"
files_modified:
  - editor.html
autonomous: false
requirements:
  - PAL-03
  - PAL-04

must_haves:
  truths:
    - "editor.html 中不存在 #palette-result-panel 元素"
    - "editor.html 中不存在映射模式 radio UI（mappingMode）"
    - "color palette panel 中的'应用色卡'按钮仍可点击并直接更新画布"
    - "undo 操作可恢复应用色卡前的像素状态"
  artifacts:
    - path: "editor.html"
      provides: "已移除 palette-result-panel 的所有 HTML/CSS/JS 残留"
      contains: "applyPaletteBtn"
  key_links:
    - from: "applyPaletteBtnEl (click 事件)"
      to: "applyPalette() 同步函数"
      via: "addEventListener 绑定（约第 2796 行，保留不变）"
      pattern: "applyPaletteBtnEl.*applyPalette"
---

<objective>
移除 Phase 4 创建的 `#palette-result-panel` 所有残留代码（HTML、CSS、JS），并通过浏览器验证检查点确认 Phase 04.1 的两项修复均正确工作。

本计划依赖 Plan 01 已完成（applyPalette 已改为同步破坏性实现，generateBtn alpha 过滤已修复）。

Purpose: 清理非破坏性预览面板的全部遗留代码，使 editor.html 结构简洁一致。
Output: 清理后的 `editor.html`（7 处删除）+ 人工验证确认
</objective>

<execution_context>
@/Users/calling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/calling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.1-phase-4/04.1-CONTEXT.md
@.planning/phases/04.1-phase-4/04.1-RESEARCH.md
@.planning/phases/04.1-phase-4/04.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 palette-result-panel 全部 HTML/CSS/JS 残留</name>
  <files>editor.html</files>
  <action>
在 editor.html 中执行以下 7 处删除（按研究报告行号定位，行号可能因 Plan 01 修改而略有偏移，以实际内容为准）：

**删除 1：HTML — palette-result-panel 元素（约第 678–695 行）**
删除以下整段 HTML（含注释行）：
```html
<!-- PAL-02: Palette result panel (side-by-side diff) -->
<div id="palette-result-panel" ...>
  ...（含 before/after canvas、pal-dl-* 下载链接、pal-result-close 按钮）...
</div>
```
定位方式：搜索 `id="palette-result-panel"`，找到其开标签和对应闭标签，将整个 div 连同注释一起删除。

**删除 2：CSS — palette-result-panel scrollbar 规则（约第 136–142 行）**
删除以下 CSS 规则（共 4 条）：
```css
#palette-result-panel::-webkit-scrollbar { ... }
#palette-result-panel::-webkit-scrollbar-track { ... }
#palette-result-panel::-webkit-scrollbar-thumb { ... }
#palette-result-panel .btn-download { display: block; }
```
定位方式：搜索 `#palette-result-panel::-webkit-scrollbar`，找到起始位置，删除直到 `#palette-result-panel .btn-download` 规则结束。

**删除 3：HTML — mappingMode 映射模式 UI（约第 585–611 行）**
删除包含 3 个 radio 输入的映射模式 section 整段 HTML：
```html
<div>
  <div class="section-label">映射模式</div>
  <div class="mode-group">
    <label>...</label>  <!-- 向量 -->
    <label>...</label>  <!-- 色卡替换 -->
    <label>...</label>  <!-- 感知匹配 -->
  </div>
</div>
```
定位方式：搜索 `name="mappingMode"` 或 `映射模式`，找到父 div，删除整段。

**删除 4：JS — pal-result-close 按钮绑定（约第 2801–2806 行）**
删除以下代码块：
```javascript
const palResultCloseEl = document.getElementById('pal-result-close');
if (palResultCloseEl) {
  palResultCloseEl.addEventListener('click', () => {
    document.getElementById('palette-result-panel').style.display = 'none';
  });
}
```
定位方式：搜索 `pal-result-close`。

**删除 5：JS — modeSwapOption 显隐逻辑 A（约第 2580–2589 行）**
删除 `modeSwapOption` 相关的 JS 代码块（控制映射模式 UI 显隐）。
定位方式：搜索 `modeSwapOption`，删除所有出现处（含所在 if 块）。

**删除 6：JS — modeSwapOption 初始化（约第 2812–2814 行）**
删除 `modeSwapOption` 初始化调用（若为独立语句）。
定位方式：同上，确认所有 `modeSwapOption` 引用已删除。

**删除 7（条件删除）：JS — applyPalette 旧函数中对 palette-result-panel 的 display 操作（约第 1892–1893 行）**
若 Plan 01 替换 applyPalette 时，研究报告中提到的 `palette-result-panel` display 操作仍存在（因函数已整体替换，该行通常已消失），确认不存在即可。搜索 `palette-result-panel` 验证 0 处残留。

**约束：**
- `#canvas-area` 的 `flex-direction: row` CSS **保留**（研究报告确认删除 panel 后仅剩一个子元素，布局不受影响）
- `applyPaletteBtnEl.addEventListener('click', applyPalette)` 绑定**保留**
- `applyBtn.disabled` 门控逻辑**保留**
- `palShowStatus` 函数**保留**
  </action>
  <verify>
```bash
# 验证 palette-result-panel 已从 HTML 和 JS 中全部清除
grep -c "palette-result-panel" /Users/calling/perfectPixel_ver1.1/editor.html
# 期望输出：0

# 验证 mappingMode 已清除
grep -c "mappingMode\|modeSwapOption" /Users/calling/perfectPixel_ver1.1/editor.html
# 期望输出：0

# 验证 applyPalette 绑定仍存在
grep -n "applyPalette" /Users/calling/perfectPixel_ver1.1/editor.html
# 应看到：函数定义 + addEventListener 绑定两处（不含 async 关键字、不含 fetch）

# 验证 nearestPaletteColor 仍存在（来自 Plan 01）
grep -n "nearestPaletteColor" /Users/calling/perfectPixel_ver1.1/editor.html
# 应看到：函数定义 + 调用两处
```
  </verify>
  <done>editor.html 中 `palette-result-panel` 字符串出现次数为 0；`mappingMode` 和 `modeSwapOption` 出现次数为 0；`applyPalette` 仍作为同步函数存在并有 click 绑定</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 浏览器目视验证 — Phase 04.1 全部成功标准确认</name>
  <action>人工在浏览器中验证 Phase 04.1 的四项成功标准均已满足：色卡生成无虚假黑色、应用色卡直接写入画布、操作可撤销、预览面板已移除。</action>
  <what-built>
Plan 01 完成了 PAL-03（alpha 过滤修正）和 PAL-04（破坏性应用色卡 + pushHistory）；Plan 02 Task 1 移除了 palette-result-panel 及所有 mappingMode 残留代码。
  </what-built>
  <how-to-verify>
确保 Flask 运行在端口 5010（`python3 web_app.py`），然后执行以下验证步骤：

**验证 1 — 色卡生成不再产生虚假黑色**
1. 打开 `http://localhost:5010/editor`
2. 载入一张含透明或半透明像素的 PNG（例如：带有透明背景的像素画）
3. 在左侧色卡面板中，设置颜色数量（如 8），点击"生成色卡"
4. 观察生成的色卡：**不应出现黑色或接近黑色的色号**（除非原图本身有黑色像素）

**验证 2 — 应用色卡直接更新画布**
1. 色卡生成后，点击"应用色卡"按钮
2. 观察：**画布内容直接更新**，颜色被替换为色卡中的最近色
3. 原来透明/半透明区域（alpha ≤ 127）保持透明（棋盘格显示区域）
4. **右侧预览面板不再出现**（#palette-result-panel 已移除）

**验证 3 — 应用色卡可撤销**
1. 应用色卡后，按 `Cmd+Z`
2. 画布恢复到应用色卡前的状态（像素完全还原）
3. 按 `Shift+Cmd+Z` 重做，画布再次显示色卡应用后的状态

**验证 4 — 无 JS 报错**
打开浏览器 DevTools → Console，确认无红色错误信息（特别是无 `mappingMode`、`palette-result-panel`、`pal-result-close` 相关的 TypeError）。
  </how-to-verify>
  <resume-signal>输入 "approved" 确认所有验证通过，或描述发现的问题</resume-signal>
</task>

</tasks>

<verification>
Task 1（代码清理）完成条件：
- `grep -c "palette-result-panel" editor.html` 输出 0
- `grep -c "mappingMode\|modeSwapOption" editor.html` 输出 0
- `grep -n "async function applyPalette" editor.html` 无输出
- `grep -n "nearestPaletteColor" editor.html` 有至少 2 处（定义 + 调用）

Checkpoint 通过条件：
- 含透明像素的图片生成色卡后，色卡中无虚假黑色/灰色条目
- 点击"应用色卡"后画布直接更新（无弹窗、无右侧预览面板）
- Cmd+Z 完全还原应用色卡操作
- DevTools Console 无 JS 错误
</verification>

<success_criteria>
Phase 04.1 全部成功标准（来自 ROADMAP.md）：
1. 含半透明像素的图片生成色卡，只有 alpha ≥ 128 的像素参与颜色统计；alpha ≤ 127 的像素不产生任何色号条目
2. 点击"应用色卡"后画布内容直接被替换（EditorState.pixels 更新，flushPixels 重绘）；alpha ≥ 128 → 最近色卡色（alpha=255），alpha ≤ 127 → 纯透明 (0,0,0,0)
3. 应用色卡操作可通过 Cmd+Z 撤销，完全还原到应用前的像素状态
4. 右侧的非破坏性预览面板（原 palette-result-panel）已从 editor.html 移除
</success_criteria>

<output>
完成后创建 `.planning/phases/04.1-phase-4/04.1-02-SUMMARY.md`，记录：
- 7 处删除的实际行号范围
- 人工验证结果（通过/发现问题）
- Phase 04.1 整体完成状态
</output>
