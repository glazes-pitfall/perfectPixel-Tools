---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - editor.html
autonomous: true
requirements:
  - CANVAS-01
  - CANVAS-02

must_haves:
  truths:
    - "editor.html 加载后自动从 /output.png 获取图片，显示在中央画布区"
    - "canvas 以 image-rendering:pixelated 渲染，无插值模糊"
    - "pixel-canvas 尺寸严格等于图片像素尺寸（无 DPR 缩放）"
    - "悬停在画布上时，像素检查器实时显示 X/Y 坐标和正确的 RGBA 值（从 EditorState.pixels 读取）"
    - "滚轮 / Ctrl+= / Ctrl+- / 顶部栏 +/- 按钮均可缩放，范围 1x–64x"
    - "缩放以鼠标指针为中心保持不动"
    - "zoom-container 内的棋盘格随缩放等比变化（16 canvas-px 一格）"
    - "画布小于视口时居中显示；大于视口时可用滚动条平移"
  artifacts:
    - path: "editor.html"
      provides: "完整 Phase 1 功能（canvas + zoom + pixel inspector）"
      min_lines: 300
      contains: "willReadFrequently"
    - path: "editor.html"
      provides: "三层 canvas 正确初始化"
      contains: "pixel-canvas"
  key_links:
    - from: "loadPlaceholderImage()"
      to: "EditorState.pixels"
      via: "fetch('/output.png') → createImageBitmap → getImageData → .slice()"
      pattern: "EditorState.pixels.*slice"
    - from: "cursorCanvas pointermove"
      to: "pixel-inspector DOM"
      via: "viewportToCanvas → getPixel → 更新 insp-* span"
      pattern: "insp-x.*textContent"
    - from: "wheel event"
      to: "zoom-container CSS transform"
      via: "applyZoom → style.transform = `scale(${newZoom})`"
      pattern: "style.transform.*scale"
---

<objective>
在 Plan 01 创建的 editor.html 骨架上，实现完整的三层 canvas 初始化、EditorState 像素缓冲区、图片加载、缩放系统和像素检查器。这是 Phase 1 的核心技术层，后续所有 Phase 的工具和逻辑都依赖本计划建立的坐标系和像素读写路径。

Purpose: 正确实现 canvas 初始化顺序（DPR 规则、willReadFrequently）和缩放系统是 Phase 1 "必须从第一天就做对" 的要求。
Output: editor.html（在 Plan 01 骨架上，JS 函数全部实现）
</objective>

<execution_context>
@/Users/calling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/calling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01 在 editor.html 中建立的 DOM 节点（本计划必须引用这些 ID） -->

HTML 节点（Plan 01 已创建，本计划 JS 必须 getElementById 引用）：
- `#zoom-container`  ← CSS transform 作用目标（transform-origin:top left 已在 Plan 01 设置）
- `#zoom-outer`      ← flex 居中容器，包裹 zoom-container
- `#canvas-area`     ← overflow:auto 滚动容器（wheel 事件监听器绑定于此）
- `#zoom-display`    ← 显示当前 zoom 倍率文字（如 "4x"）
- `#btn-zoom-in`     ← Zoom + 按钮（Plan 01 已创建，本计划绑定 click handler）
- `#btn-zoom-out`    ← Zoom - 按钮（Plan 01 已创建，本计划绑定 click handler）
- `#insp-x`, `#insp-y`, `#insp-r`, `#insp-g`, `#insp-b`, `#insp-a` ← 检查器数值 span
- `#insp-swatch`     ← 检查器颜色预览色块

JS 骨架（Plan 01 已声明的空函数，本计划必须用完整实现**替换**这些占位）：
```javascript
function initCanvases(width, height) {}       // ← 替换为完整实现
function flushPixels() {}                      // ← 替换为完整实现
function viewportToCanvas(clientX, clientY)    // ← 替换为完整实现
function getPixel(x, y)                        // ← 替换为完整实现
function applyZoom(newZoom, pivotX, pivotY)   // ← 替换为完整实现
async function loadPlaceholderImage()          // ← 替换为完整实现
```

EditorState 对象（Plan 01 已声明，本计划直接读写，不重新声明）：
```javascript
const EditorState = {
  width: 0, height: 0,
  pixels: null,               // Uint8ClampedArray, RGBA, length = w*h*4
  gridW: 0, gridH: 0,
  zoom: 4, panX: 0, panY: 0,
  activeTool: 'pencil',
  foregroundColor: [0, 0, 0, 255],
  toolOptions: { brushSize:1, brushShape:'round', pixelPerfect:false, bucketTolerance:15, wandTolerance:15, contiguous:true },
  selection: null, selectionPixels: null, transformState: null,
  history: [], historyIndex: -1, MAX_HISTORY: 50,
  palette: [],
};
```
</interfaces>

<!-- 关键实现模式（从 RESEARCH.md 提取，executor 直接使用） -->

**Pattern 1: 三层 Canvas 初始化（必须严格遵守 DPR 规则）**
```javascript
// pixel-canvas: 必须无 DPR。宽高 = 图片像素尺寸
pixelCanvas.width = width;
pixelCanvas.height = height;
pixelCanvas.style.width  = width  + 'px';
pixelCanvas.style.height = height + 'px';
// willReadFrequently 必须在 FIRST getContext 调用时设置（无法事后补设）
const pixelCtx = pixelCanvas.getContext('2d', { willReadFrequently: true, alpha: true });

// overlay canvases: DPR 缩放（让光标/选区在 Retina 屏清晰）
const dpr = window.devicePixelRatio || 1;
[selCanvas, cursorCanvas].forEach(canvas => {
  canvas.width  = width  * dpr;
  canvas.height = height * dpr;
  canvas.style.width  = width  + 'px';
  canvas.style.height = height + 'px';
});
const selCtx    = selCanvas.getContext('2d');
const cursorCtx = cursorCanvas.getContext('2d');
selCtx.scale(dpr, dpr);
cursorCtx.scale(dpr, dpr);
```

**Pattern 2: 鼠标指针居中缩放**
```javascript
function applyZoom(newZoom, pivotClientX, pivotClientY) {
  const oldZoom = EditorState.zoom;
  newZoom = Math.max(1, Math.min(64, newZoom));
  const area = document.getElementById('canvas-area');
  const rect  = area.getBoundingClientRect();
  const pivotX = pivotClientX - rect.left + area.scrollLeft;
  const pivotY = pivotClientY - rect.top  + area.scrollTop;
  area.scrollLeft = pivotX * (newZoom / oldZoom) - (pivotClientX - rect.left);
  area.scrollTop  = pivotY * (newZoom / oldZoom) - (pivotClientY - rect.top);
  EditorState.zoom = newZoom;
  document.getElementById('zoom-container').style.transform = `scale(${newZoom})`;
  document.getElementById('zoom-display').textContent = newZoom + 'x';
}
```

**Pattern 3: 坐标转换**
```javascript
function viewportToCanvas(clientX, clientY) {
  const rect = cursorCanvas.getBoundingClientRect();
  const scaleX = EditorState.width  / rect.width;
  const scaleY = EditorState.height / rect.height;
  return [
    Math.max(0, Math.min(EditorState.width  - 1, Math.floor((clientX - rect.left) * scaleX))),
    Math.max(0, Math.min(EditorState.height - 1, Math.floor((clientY - rect.top)  * scaleY))),
  ];
}
```

**Pattern 4: 图片加载（一次性 getImageData，之后不再调用）**
```javascript
async function loadPlaceholderImage() {
  const resp   = await fetch('/output.png');
  const blob   = await resp.blob();
  const bitmap = await createImageBitmap(blob);
  const { width, height } = bitmap;
  initCanvases(width, height);
  // 这是唯一一次 getImageData（初始化加载），后续所有读取通过 EditorState.pixels
  pixelCtx.drawImage(bitmap, 0, 0);
  const imageData = pixelCtx.getImageData(0, 0, width, height);
  EditorState.pixels = imageData.data.slice(); // Uint8ClampedArray 副本
  flushPixels(); // 通过 EditorState.pixels 重新渲染（建立正规路径）
}
```

**Pattern 5: 棋盘格 CSS（缩放等比变化，16 canvas-px 一格）**
```css
/* 棋盘格应用于 #zoom-container（在 CSS transform 内），使其随缩放等比变化 */
#zoom-container {
  background-color: var(--surface2);
  background-image:
    linear-gradient(45deg, var(--surface) 25%, transparent 25%),
    linear-gradient(-45deg, var(--surface) 25%, transparent 25%),
    linear-gradient(45deg, transparent 75%, var(--surface) 75%),
    linear-gradient(-45deg, transparent 75%, var(--surface) 75%);
  background-size: 32px 32px;   /* 2 × 16px，zoom=1 时 16 CSS px/格，zoom=4 时 64 CSS px/格 */
  background-position: 0 0, 0 16px, 16px -16px, -16px 0px;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现三层 Canvas 初始化、EditorState 像素缓冲区、图片加载</name>
  <files>editor.html</files>
  <action>
修改 editor.html，**替换** Plan 01 中的空函数占位，实现以下内容：

**1. 在 `#zoom-container` 的内联样式中添加棋盘格背景（CSS 部分）：**
更新 Plan 01 创建的 `#zoom-container` 样式规则（`<style>` 中），添加棋盘格背景（Pattern 5）：
```css
#zoom-container {
  position: relative;
  transform-origin: top left;
  background-color: var(--surface2);
  background-image:
    linear-gradient(45deg, var(--surface) 25%, transparent 25%),
    linear-gradient(-45deg, var(--surface) 25%, transparent 25%),
    linear-gradient(45deg, transparent 75%, var(--surface) 75%),
    linear-gradient(-45deg, transparent 75%, var(--surface) 75%);
  background-size: 32px 32px;
  background-position: 0 0, 0 16px, 16px -16px, -16px 0px;
}
```

**2. 在 `#canvas-area` 的 HTML 中，在 `#zoom-container` 内创建三层 canvas：**
```html
<canvas id="pixel-canvas"></canvas>
<canvas id="selection-canvas"></canvas>
<canvas id="cursor-canvas"></canvas>
```
（三个 `<canvas>` 标签放在 `<div id="zoom-container">` 内，无内联 style；所有尺寸由 JS 的 initCanvases 设置）

**3. 在 `<style>` 中添加 canvas 通用样式：**
```css
canvas {
  image-rendering: pixelated;
  image-rendering: crisp-edges;
  display: block;
}
#pixel-canvas, #selection-canvas, #cursor-canvas {
  position: absolute;
  top: 0;
  left: 0;
}
#pixel-canvas     { z-index: 1; }
#selection-canvas { z-index: 2; }
#cursor-canvas    { z-index: 3; cursor: crosshair; }
```

**4. 在 `<script>` 中声明 canvas 和 ctx 变量（在 EditorState 之后、initCanvases 之前）：**
```javascript
// Canvas 元素引用（DOM ready 后有效，通过 initCanvases 实际设置尺寸）
let pixelCanvas, selCanvas, cursorCanvas;
let pixelCtx, selCtx, cursorCtx;
```

**5. 实现 `initCanvases(width, height)` 函数（替换空占位）：**

将 Pattern 1 的代码写入函数体，**但必须将 `const pixelCtx/selCtx/cursorCtx` 改为直接赋值（省略 `const`/`let`），以更新 step 4 声明的模块级 `let` 变量**。否则 `flushPixels()` 等函数中引用的模块级 `pixelCtx` 将为 `undefined`，导致运行时 TypeError。

正确写法示例（注意无 `const`）：
```javascript
function initCanvases(width, height) {
  EditorState.width  = width;
  EditorState.height = height;

  // pixel-canvas: 无 DPR，宽高 = 图片像素尺寸
  pixelCanvas.width  = width;
  pixelCanvas.height = height;
  pixelCanvas.style.width  = width  + 'px';
  pixelCanvas.style.height = height + 'px';
  // willReadFrequently 必须在首次 getContext 调用时设置（无法事后补设）
  pixelCtx = pixelCanvas.getContext('2d', { willReadFrequently: true, alpha: true });
  // ↑ 赋值给模块级 let pixelCtx，不要写 const pixelCtx = ...

  // overlay canvases: DPR 缩放（Retina 屏清晰）
  const dpr = window.devicePixelRatio || 1;
  [selCanvas, cursorCanvas].forEach(canvas => {
    canvas.width  = width  * dpr;
    canvas.height = height * dpr;
    canvas.style.width  = width  + 'px';
    canvas.style.height = height + 'px';
  });
  selCtx    = selCanvas.getContext('2d');    // 赋值给模块级 let selCtx
  cursorCtx = cursorCanvas.getContext('2d'); // 赋值给模块级 let cursorCtx
  selCtx.scale(dpr, dpr);
  cursorCtx.scale(dpr, dpr);
}
```

**关键要求：**
- pixel-canvas: `width/height` = 图片尺寸，**不应用 DPR**
- `getContext('2d', { willReadFrequently: true, alpha: true })` — **必须是 pixel-canvas 的首次 getContext 调用**
- overlay canvases: `width/height = 图片尺寸 × dpr`，CSS style 尺寸 = 图片尺寸（无 dpr）
- 调用 `selCtx.scale(dpr, dpr)` 和 `cursorCtx.scale(dpr, dpr)`

**6. 实现 `flushPixels()` 函数（替换空占位）：**
```javascript
function flushPixels() {
  pixelCtx.putImageData(
    new ImageData(EditorState.pixels, EditorState.width, EditorState.height),
    0, 0
  );
}
```

**7. 实现 `getPixel(x, y)` 函数（替换空占位）：**
```javascript
function getPixel(x, y) {
  const i = (y * EditorState.width + x) * 4;
  return [
    EditorState.pixels[i],
    EditorState.pixels[i+1],
    EditorState.pixels[i+2],
    EditorState.pixels[i+3],
  ];
}
```

**8. 实现 `viewportToCanvas(clientX, clientY)` 函数（替换空占位，使用 Pattern 3）**

**9. 实现 `loadPlaceholderImage()` 函数（替换空占位，使用 Pattern 4）：**
- fetch('/output.png')
- createImageBitmap
- initCanvases(bitmap.width, bitmap.height)（此步骤在 pixelCtx 分配前执行）
- **注意初始化顺序**：initCanvases 必须先于 drawImage 执行（否则 willReadFrequently 提示在 getContext 已调用后失效）
- drawImage → getImageData → `.data.slice()` → EditorState.pixels → flushPixels()
- 加载完成后更新状态文字（可选，但有助于调试）

**10. 在 `DOMContentLoaded` 处理器中获取 canvas 引用并调用 loadPlaceholderImage：**
```javascript
document.addEventListener('DOMContentLoaded', () => {
  pixelCanvas   = document.getElementById('pixel-canvas');
  selCanvas     = document.getElementById('selection-canvas');
  cursorCanvas  = document.getElementById('cursor-canvas');
  loadPlaceholderImage();
});
```

**关键约束（必须全部满足）：**
- pixel-canvas 的宽高属性（.width / .height）必须等于图片像素数（102×102），**永远不乘以 dpr**
- `willReadFrequently: true` 必须在 pixel-canvas 的 **第一次** `getContext('2d')` 调用中设置
- 所有像素读取通过 `EditorState.pixels`，不调用 `getImageData` 作为工具逻辑
- `transform-origin: top left` 必须在 `#zoom-container` 上（Plan 01 已设置，确认保留）
  </action>
  <verify>
    <automated>cd /Users/calling/perfectPixel_ver1.1 && python3 -c "
content = open('editor.html').read()
checks = [
  'willReadFrequently' in content,
  'image-rendering: pixelated' in content,
  'id=\"pixel-canvas\"' in content,
  'id=\"selection-canvas\"' in content,
  'id=\"cursor-canvas\"' in content,
  'createImageBitmap' in content,
  'EditorState.pixels' in content,
  'putImageData' in content,
  'getBoundingClientRect' in content,
  'background-size: 32px 32px' in content,
]
failed = [i for i, c in enumerate(checks) if not c]
if failed: print('FAIL checks:', failed)
else: print('ALL PASS')
"</automated>
  </verify>
  <done>editor.html 包含三层 canvas（正确 DPR 处理）、棋盘格 CSS、图片加载逻辑（从 EditorState.pixels 刷新）、坐标转换函数；pixel-canvas 尺寸不含 DPR 缩放</done>
</task>

<task type="auto">
  <name>Task 2: 实现缩放系统（滚轮 / 键盘 / 按钮）和像素检查器（悬停读值）</name>
  <files>editor.html</files>
  <action>
在 editor.html 的 `<script>` 中，继续替换/补充 Plan 01 的空函数占位，实现缩放和像素检查器：

**1. 实现 `applyZoom(newZoom, pivotClientX, pivotClientY)` 函数（使用 Pattern 2）：**
- `newZoom = Math.max(1, Math.min(64, newZoom))`
- 获取 `#canvas-area` 的 `getBoundingClientRect()`
- 计算 pivot 点在滚动坐标中的位置
- 更新 `area.scrollLeft` 和 `area.scrollTop` 使 pivot 点保持不动
- `EditorState.zoom = newZoom`
- `document.getElementById('zoom-container').style.transform = \`scale(${newZoom})\``
- `document.getElementById('zoom-display').textContent = newZoom + 'x'`

**2. 在 `DOMContentLoaded` 处理器中绑定以下事件（在 loadPlaceholderImage() 调用之后）：**

**滚轮缩放：**
```javascript
const canvasArea = document.getElementById('canvas-area');
canvasArea.addEventListener('wheel', e => {
  e.preventDefault();
  const delta = e.deltaY < 0 ? 1 : -1;
  const step = EditorState.zoom < 4 ? 1 : EditorState.zoom < 16 ? 2 : 4;
  applyZoom(EditorState.zoom + delta * step, e.clientX, e.clientY);
}, { passive: false });
```

**顶部栏 Zoom 按钮：**
```javascript
document.getElementById('btn-zoom-in').addEventListener('click', () => {
  const step = EditorState.zoom < 4 ? 1 : EditorState.zoom < 16 ? 2 : 4;
  // 按钮点击以画布区域中心为 pivot
  const rect = canvasArea.getBoundingClientRect();
  applyZoom(EditorState.zoom + step, rect.left + rect.width / 2, rect.top + rect.height / 2);
});
document.getElementById('btn-zoom-out').addEventListener('click', () => {
  const step = EditorState.zoom < 4 ? 1 : EditorState.zoom < 16 ? 2 : 4;
  const rect = canvasArea.getBoundingClientRect();
  applyZoom(EditorState.zoom - step, rect.left + rect.width / 2, rect.top + rect.height / 2);
});
```

**键盘缩放（Ctrl+= / Ctrl+-）：**
```javascript
document.addEventListener('keydown', e => {
  if (e.target.matches('input, textarea')) return; // 不拦截文字输入
  if ((e.ctrlKey || e.metaKey) && (e.key === '=' || e.key === '+')) {
    e.preventDefault();
    const step = EditorState.zoom < 4 ? 1 : EditorState.zoom < 16 ? 2 : 4;
    const rect = canvasArea.getBoundingClientRect();
    applyZoom(EditorState.zoom + step, rect.left + rect.width / 2, rect.top + rect.height / 2);
  }
  if ((e.ctrlKey || e.metaKey) && e.key === '-') {
    e.preventDefault();
    const step = EditorState.zoom < 4 ? 1 : EditorState.zoom < 16 ? 2 : 4;
    const rect = canvasArea.getBoundingClientRect();
    applyZoom(EditorState.zoom - step, rect.left + rect.width / 2, rect.top + rect.height / 2);
  }
});
```

**3. 像素检查器（pointermove → 读 EditorState.pixels，不读 canvas）：**
在 `DOMContentLoaded` 处理器中，在 canvas 引用设置之后，添加 pointermove 监听器到 cursorCanvas。
**注意**：cursorCanvas 变量在 loadPlaceholderImage 之后才有有效内容，因此监听器绑定可在 loadPlaceholderImage 异步完成后进行，或者在 DOMContentLoaded 时先绑定（canvas 元素已存在，事件可以提前注册，函数内部检查 EditorState.pixels 是否已初始化）：
```javascript
// 延迟绑定（在 loadPlaceholderImage resolve 后）或提前绑定并在回调中检查
document.addEventListener('DOMContentLoaded', () => {
  pixelCanvas  = document.getElementById('pixel-canvas');
  selCanvas    = document.getElementById('selection-canvas');
  cursorCanvas = document.getElementById('cursor-canvas');

  loadPlaceholderImage().then(() => {
    // 图片加载完成后绑定 pointermove
    cursorCanvas.addEventListener('pointermove', e => {
      if (!EditorState.pixels) return;
      const [cx, cy] = viewportToCanvas(e.clientX, e.clientY);
      const [r, g, b, a] = getPixel(cx, cy);
      document.getElementById('insp-x').textContent = cx;
      document.getElementById('insp-y').textContent = cy;
      document.getElementById('insp-r').textContent = r;
      document.getElementById('insp-g').textContent = g;
      document.getElementById('insp-b').textContent = b;
      document.getElementById('insp-a').textContent = a;
      document.getElementById('insp-swatch').style.background =
        `rgba(${r},${g},${b},${a/255})`;
    });
    // 鼠标离开时重置检查器
    cursorCanvas.addEventListener('pointerleave', () => {
      ['insp-x','insp-y','insp-r','insp-g','insp-b','insp-a'].forEach(id => {
        document.getElementById(id).textContent = '—';
      });
      document.getElementById('insp-swatch').style.background = 'transparent';
    });
  });

  // 缩放事件可以立即绑定（不依赖图片加载）
  const canvasArea = document.getElementById('canvas-area');

  // 滚轮缩放
  canvasArea.addEventListener('wheel', e => {
    e.preventDefault();
    const delta = e.deltaY < 0 ? 1 : -1;
    const step = EditorState.zoom < 4 ? 1 : EditorState.zoom < 16 ? 2 : 4;
    applyZoom(EditorState.zoom + delta * step, e.clientX, e.clientY);
  }, { passive: false });

  // 顶部栏 Zoom 按钮
  document.getElementById('btn-zoom-in').addEventListener('click', () => {
    const step = EditorState.zoom < 4 ? 1 : EditorState.zoom < 16 ? 2 : 4;
    const rect = canvasArea.getBoundingClientRect();
    applyZoom(EditorState.zoom + step, rect.left + rect.width / 2, rect.top + rect.height / 2);
  });
  document.getElementById('btn-zoom-out').addEventListener('click', () => {
    const step = EditorState.zoom < 4 ? 1 : EditorState.zoom < 16 ? 2 : 4;
    const rect = canvasArea.getBoundingClientRect();
    applyZoom(EditorState.zoom - step, rect.left + rect.width / 2, rect.top + rect.height / 2);
  });

  // 键盘缩放（Ctrl+= / Ctrl+-）
  document.addEventListener('keydown', e => {
    if (e.target.matches('input, textarea')) return;
    if ((e.ctrlKey || e.metaKey) && (e.key === '=' || e.key === '+')) {
      e.preventDefault();
      const step = EditorState.zoom < 4 ? 1 : EditorState.zoom < 16 ? 2 : 4;
      const rect = canvasArea.getBoundingClientRect();
      applyZoom(EditorState.zoom + step, rect.left + rect.width / 2, rect.top + rect.height / 2);
    }
    if ((e.ctrlKey || e.metaKey) && e.key === '-') {
      e.preventDefault();
      const step = EditorState.zoom < 4 ? 1 : EditorState.zoom < 16 ? 2 : 4;
      const rect = canvasArea.getBoundingClientRect();
      applyZoom(EditorState.zoom - step, rect.left + rect.width / 2, rect.top + rect.height / 2);
    }
  });
});
```

**关键约束：**
- 滚轮事件必须使用 `{ passive: false }` 选项，否则 `e.preventDefault()` 被忽略（页面会滚动）
- 键盘快捷键必须检查 `e.target.matches('input, textarea')` 再处理（避免干扰文字输入）
- 像素检查器读取 `EditorState.pixels` 而非 `getImageData`（premultiplied alpha 规则）
- pointerleave 时重置检查器显示（UX 一致性）
  </action>
  <verify>
    <automated>cd /Users/calling/perfectPixel_ver1.1 && python3 -c "
content = open('editor.html').read()
checks = [
  'passive: false' in content,
  'preventDefault' in content,
  'pointermove' in content,
  'pointerleave' in content,
  'insp-x' in content,
  'applyZoom' in content,
  'btn-zoom-in' in content,
  'btn-zoom-out' in content,
  'keydown' in content,
  'matches.*input.*textarea' in content or \"matches('input\" in content,
]
failed = [i for i, c in enumerate(checks) if not c]
if failed: print('FAIL checks:', failed)
else: print('ALL PASS')
"</automated>
  </verify>
  <done>缩放系统（滚轮/键盘/按钮）和像素检查器（pointermove）完整实现；滚轮使用 passive:false 防止页面滚动；键盘快捷键正确过滤 input/textarea</done>
</task>

</tasks>

<verification>
1. 运行 Flask（端口 5010），访问 http://localhost:5010/editor
2. 页面加载后，中央画布区显示 output.png 像素图像，无模糊（image-rendering:pixelated 生效）
3. 棋盘格在画布后方可见，缩放时格子大小随之等比变化
4. 悬停在画布像素上，左侧面板底部的像素检查器实时显示 X/Y/R/G/B/A 值
5. 使用滚轮缩放，画布以鼠标指针为中心放大/缩小，不发生页面滚动
6. 顶部栏 +/- 按钮可以缩放，zoom 显示数字正确更新
7. Ctrl+= / Ctrl+- 键盘快捷键生效
8. 浏览器控制台无 JS 错误
9. 在 DevTools 中检查 pixel-canvas：`.width` 和 `.height` 等于图片像素数（102），`.style.width` 和 `.style.height` 也是 "102px"（无 DPR 缩放）
</verification>

<success_criteria>
- 三层 canvas 正确初始化（pixel-canvas 无 DPR，overlay canvases 有 DPR）
- `willReadFrequently: true` 在 pixel-canvas 首次 getContext 调用时设置
- output.png 加载并正确显示在 canvas 上，`EditorState.pixels` 包含有效 Uint8ClampedArray
- 缩放系统完整（滚轮/按钮/键盘，范围 1x–64x，鼠标中心）
- 像素检查器悬停显示正确的 RGBA 值（从 EditorState.pixels 读取，非从 canvas getImageData）
- image-rendering: pixelated 无插值模糊
</success_criteria>

<output>
完成后，创建 `.planning/phases/01-foundation/01-02-SUMMARY.md`，记录：
- 三层 canvas 初始化细节（DPR 值、尺寸设置、willReadFrequently 位置）
- 图片加载路径（fetch → createImageBitmap → EditorState.pixels）
- 缩放实现（applyZoom 中的 scroll 数学）
- 像素检查器实现（绑定时机、getPixel 路径）
- 任何与计划偏差的说明
</output>
