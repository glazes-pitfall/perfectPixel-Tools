---
phase: 04-palette-panel
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified: []
autonomous: false
requirements:
  - PAL-01
  - PAL-02

must_haves:
  truths:
    - "色卡限制面板在 editor.html 完整可用"
    - "点击色块立即更新调色盘颜色"
    - "调色盘颜色与色块 RGB 精确匹配时出现发光高亮"
    - "应用色卡后显示非破坏性结果预览"
    - "web_ui.html 原始色卡面板已删除"
  artifacts:
    - path: "editor.html"
      provides: "Phase 4 完整功能：色卡面板 + PAL-01 + PAL-02 + 结果预览"
      contains: "highlightMatchingSwatches"
    - path: "web_ui.html"
      provides: "色卡面板代码已清除"
  key_links:
    - from: "swatch 单击"
      to: "调色盘前景色 + 高亮刷新"
      via: "EditorState.foregroundColor → syncColorUI() → highlightMatchingSwatches()"
      pattern: "syncColorUI.*highlightMatchingSwatches"
---

<objective>
对 Phase 4 全部成果进行浏览器目视验证，确认所有 ROADMAP 成功标准均满足。

Purpose: 人工确认 PAL-01、PAL-02、UI-03 及删除 web_ui.html 色卡面板的视觉效果，发现自动化检查无法捕捉的 UI 问题。
Output: Phase 4 验证通过或输出具体缺陷描述。
</objective>

<execution_context>
@/Users/calling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/calling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/calling/perfectPixel_ver1.1/.planning/ROADMAP.md
@/Users/calling/perfectPixel_ver1.1/.planning/phases/04-palette-panel/04-01-SUMMARY.md
@/Users/calling/perfectPixel_ver1.1/.planning/phases/04-palette-panel/04-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 4 完整功能已实现：
- editor.html 左栏：完整色卡限制面板（生成色卡、上传色卡文件、保存/加载、映射模式、应用色卡）
- PAL-01：点击色块 → 调色盘前景色立即同步
- PAL-02：调色盘颜色与色块精确 RGB 匹配时显示发光高亮
- UI-03：应用色卡后显示原图 + 映射结果非破坏性预览，含精准版/放大版下载
- web_ui.html：色卡面板代码已删除，核心功能保留
  </what-built>
  <how-to-verify>
确保 Flask 运行（`python3 web_app.py`），然后按以下步骤验证：

**步骤 1 — 打开编辑器**
访问 http://localhost:5010/editor，加载任意一张像素图（可用默认的 output.png）

**步骤 2 — 验证色卡面板外观**
确认左栏「色卡限制」面板可见，点击 toggle 开启，参数区**自动展开**（不需要额外点击）

**步骤 3 — 生成色卡（PAL-01 前提）**
在色卡数量输入框输入 8，点击「生成色卡」，等待 8 个色块出现在色块区

**步骤 4 — 验证 PAL-01**
点击任意一个色块，观察调色盘（左下角色轮）颜色是否立即变化为该色块颜色。点击不同色块，颜色应随之切换。工具不应切换（铅笔仍激活）

**步骤 5 — 验证 PAL-02**
确认当前调色盘颜色与某个色块颜色相同时，该色块有发光效果（白色内圈 + 彩色外晕）。点击不同色块，高亮跟随变化。在调色盘中手动输入一个不在色卡中的颜色（hex 框），确认所有色块高亮消失

**步骤 6 — 验证应用色卡（UI-03）**
确保色块已生成（步骤 3），点击「应用色卡」按钮：
- 按钮显示「处理中...」状态
- 画布右侧出现「色卡结果」面板，显示原图（左）和色卡映射结果（右）
- 两张图均以 `image-rendering: pixelated` 渲染（无模糊）
- 精准版/放大版下载链接存在且可点击（触发文件下载）
- 按 Cmd+Z 撤销：历史步数不增加（应用色卡不计入撤销）

**步骤 7 — 验证 web_ui.html 删除**
访问 http://localhost:5010（主界面），确认页面无「色卡限制」相关区块，上传图片并像素化处理功能正常
  </how-to-verify>
  <resume-signal>输入 "approved" 确认通过，或描述具体问题（如「步骤 4 中点击色块调色盘未更新」）</resume-signal>
</task>

</tasks>

<verification>
Phase 4 ROADMAP 成功标准核对：
1. ✓ 点击色卡色块 → 调色盘立即更新（PAL-01 步骤 4 验证）
2. ✓ 调色盘颜色匹配色块时显示发光高亮（PAL-02 步骤 5 验证）
3. ✓ 面板可折叠/展开不丢失色卡数据（步骤 2 验证）
</verification>

<success_criteria>
所有 7 个验证步骤通过，用户输入 "approved"。Phase 4 完成。
</success_criteria>

<output>
完成后创建 `.planning/phases/04-palette-panel/04-03-SUMMARY.md`
</output>
