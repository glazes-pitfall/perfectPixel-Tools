---
phase: 04-palette-panel
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - editor.html
  - web_ui.html
autonomous: true
requirements:
  - PAL-02

must_haves:
  truths:
    - "调色盘前景色变化时，精确匹配（RGB三通道相等）的色块出现发光高亮效果"
    - "多个相同 RGB 的色块同时高亮"
    - "应用色卡后画布右侧出现原图 + 色卡映射结果的对比视图，含精准版下载按钮"
    - "应用色卡不修改 EditorState.pixels，撤销历史不变"
    - "web_ui.html 中的色卡面板 HTML/CSS/JS 已完整删除"
  artifacts:
    - path: "editor.html"
      provides: "PAL-02 highlightMatchingSwatches()、applyPalette() 完整实现、#palette-result-panel 结果区"
      contains: "highlightMatchingSwatches"
    - path: "web_ui.html"
      provides: "色卡面板代码已删除"
      contains: "<!-- palette section removed"
  key_links:
    - from: "syncColorUI()"
      to: "highlightMatchingSwatches()"
      via: "tail call after _syncLock = false"
      pattern: "highlightMatchingSwatches\\(\\)"
    - from: "applyPaletteBtn click"
      to: "/api/apply-palette"
      via: "fetch POST with FormData (image=getEditorImageB64(), palette, mode, scale)"
      pattern: "fetch.*apply-palette"
---

<objective>
实现 PAL-02（调色盘颜色匹配高亮）、applyPalette 完整逻辑（非破坏性预览 + 结果对比面板），并从 web_ui.html 删除色卡面板代码。

Purpose: 完成双向同步闭环（色块→调色盘 已在 Plan 01 完成；调色盘→色块高亮 在本计划完成），同时实现 UI-03 应用色卡结果预览。
Output: highlightMatchingSwatches() 函数；#palette-result-panel 结果对比区；web_ui.html 色卡代码清除。
</objective>

<execution_context>
@/Users/calling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/calling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/calling/perfectPixel_ver1.1/.planning/phases/04-palette-panel/04-CONTEXT.md
@/Users/calling/perfectPixel_ver1.1/.planning/phases/04-palette-panel/04-RESEARCH.md
@/Users/calling/perfectPixel_ver1.1/.planning/phases/04-palette-panel/04-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 output and existing editor.html structure. -->

From editor.html after Plan 01:
```javascript
// syncColorUI() — 在 _syncLock = false 后 添加 tail hook:
function syncColorUI() {
  if (_syncLock) return;
  _syncLock = true;
  // ... existing logic ...
  _syncLock = false;
  highlightMatchingSwatches(); // ← 在此处添加（Plan 02 任务）
}

// PAL-02 函数（Plan 02 新增）:
function highlightMatchingSwatches() {
  if (!EditorState.pixels) return;
  const [fr, fg, fb] = EditorState.foregroundColor;
  document.querySelectorAll('#swatchesGrid .swatch').forEach((sw, idx) => {
    if (idx >= currentPalette.length) return;
    const [r, g, b] = currentPalette[idx];
    if (r === fr && g === fg && b === fb) {
      sw.style.boxShadow = `0 0 0 2px #fff, 0 0 8px 4px rgb(${r},${g},${b})`;
      sw.style.zIndex = '3';
    } else {
      sw.style.boxShadow = '';
      sw.style.zIndex = '';
    }
  });
}

// getEditorImageB64() — Plan 02 新增:
function getEditorImageB64() {
  if (!EditorState.pixels || !EditorState.width || !EditorState.height) return null;
  const off = document.createElement('canvas');
  off.width = EditorState.width; off.height = EditorState.height;
  const octx = off.getContext('2d');
  octx.putImageData(new ImageData(EditorState.pixels.slice(), EditorState.width, EditorState.height), 0, 0);
  return off.toDataURL('image/png').replace(/^data:image\/png;base64,/, '');
}
```

From editor.html HTML (canvas area — sibling placement):
```html
<div id="canvas-area">           <!-- flex-direction: row -->
  <div id="zoom-scroll-content"> ... </div>
  <!-- Plan 02 在此处插入 #palette-result-panel -->
  <div id="palette-result-panel" style="display:none; ..."></div>
</div>
```

Flask routes (already exist in web_app.py, no changes needed):
- POST /api/apply-palette  → body: FormData(image, palette, mode, scale) → JSON {output, output_scaled}
- POST /api/generate-palette → body: FormData(image, count, algorithm) → JSON {palette}
- POST /api/export-palette → body: FormData(palette, format) → blob binary file
- POST /api/parse-palette → body: FormData(file) → JSON {palette}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PAL-02 高亮 + applyPalette 完整实现 + 结果面板</name>
  <files>editor.html</files>
  <action>
**A. 添加 `highlightMatchingSwatches()` 函数（在 syncColorUI 定义之后）：**
```javascript
function highlightMatchingSwatches() {
  if (!EditorState.pixels) return;
  const [fr, fg, fb] = EditorState.foregroundColor;
  document.querySelectorAll('#swatchesGrid .swatch').forEach((sw, idx) => {
    if (idx >= currentPalette.length) return;
    const [r, g, b] = currentPalette[idx];
    if (r === fr && g === fg && b === fb) {
      sw.style.boxShadow = '0 0 0 2px #fff, 0 0 8px 4px rgb(' + r + ',' + g + ',' + b + ')';
      sw.style.zIndex = '3';
    } else {
      sw.style.boxShadow = '';
      sw.style.zIndex = '';
    }
  });
}
```

**B. 在 `syncColorUI()` 末尾（`_syncLock = false;` 之后）追加调用：**
```javascript
_syncLock = false;
highlightMatchingSwatches(); // PAL-02: 每次颜色变化后刷新高亮
```
注意：必须在 `_syncLock = false;` 之后，不在锁内调用。

**C. 同步调用点：** 在 `setCurrentPalette()` 的 `renderSwatches()` 调用之后也调用 `highlightMatchingSwatches()`，确保重新生成色卡后高亮立即生效。

**D. 添加 `getEditorImageB64()` 工具函数（在 flushPixels 附近）：**
```javascript
function getEditorImageB64() {
  if (!EditorState.pixels || !EditorState.width || !EditorState.height) return null;
  const off = document.createElement('canvas');
  off.width = EditorState.width;
  off.height = EditorState.height;
  const octx = off.getContext('2d');
  octx.putImageData(
    new ImageData(EditorState.pixels.slice(), EditorState.width, EditorState.height),
    0, 0
  );
  return off.toDataURL('image/png').replace(/^data:image\/png;base64,/, '');
}
```

**E. 添加 `#palette-result-panel` HTML（在 `#canvas-area` 内，`#zoom-scroll-content` 之后）：**
```html
<div id="palette-result-panel" style="display:none; flex-direction:column; gap:8px; padding:12px; flex-shrink:0; border-left:1px solid var(--border); overflow-y:auto; min-width:280px;">
  <div style="font-size:12px; font-weight:600; color:var(--text-muted);">色卡结果</div>
  <div style="display:flex; flex-direction:column; gap:16px;">
    <div>
      <div style="font-size:11px; color:var(--text-muted); margin-bottom:4px;">原图</div>
      <img id="pal-result-original" style="image-rendering:pixelated; max-width:240px; display:block; border:1px solid var(--border); border-radius:4px;">
      <a id="pal-dl-orig-exact" class="btn-download" style="display:block; margin-top:4px;" download="original.png">&#8595; 下载精准版</a>
    </div>
    <div>
      <div style="font-size:11px; color:var(--text-muted); margin-bottom:4px;">色卡映射</div>
      <img id="pal-result-mapped" style="image-rendering:pixelated; max-width:240px; display:block; border:1px solid var(--border); border-radius:4px;">
      <a id="pal-dl-mapped-exact" class="btn-download" style="display:block; margin-top:4px;" download="output_palette.png">&#8595; 下载精准版</a>
      <a id="pal-dl-mapped-scaled" class="btn-download" style="display:block; margin-top:4px;" download="output_palette_scaled.png">&#8595; 下载放大版</a>
    </div>
  </div>
  <button id="pal-result-close" class="btn btn-ghost" style="font-size:11px; margin-top:4px; width:100%;">关闭结果</button>
</div>
```

**F. 实现 `applyPalette()` 完整函数（替换 Plan 01 的 stub）：**
```javascript
async function applyPalette() {
  const b64 = getEditorImageB64();
  if (!b64 || currentPalette.length === 0) return;
  const btn = document.getElementById('applyPaletteBtn');
  btn.disabled = true;
  btn.textContent = '处理中...';
  try {
    const mode = document.querySelector('input[name=mappingMode]:checked')?.value || 'nearest';
    const fd = new FormData();
    fd.append('image', b64);
    fd.append('palette', JSON.stringify(currentPalette));
    fd.append('mode', mode);
    fd.append('scale', '4');
    const res = await fetch('/api/apply-palette', { method: 'POST', body: fd });
    const data = await res.json();
    if (!res.ok) { showStatus('色卡应用失败: ' + (data.error || res.status)); return; }
    // Preview only — EditorState.pixels NOT modified
    const exactSrc  = 'data:image/png;base64,' + data.output;
    const scaledSrc = 'data:image/png;base64,' + (data.output_scaled || data.output);
    const origSrc   = 'data:image/png;base64,' + b64;
    document.getElementById('pal-result-original').src = origSrc;
    document.getElementById('pal-dl-orig-exact').href  = origSrc;
    document.getElementById('pal-result-mapped').src   = exactSrc;
    document.getElementById('pal-dl-mapped-exact').href  = exactSrc;
    document.getElementById('pal-dl-mapped-scaled').href = scaledSrc;
    document.getElementById('palette-result-panel').style.display = 'flex';
  } catch (err) {
    showStatus('色卡应用出错: ' + err.message);
  } finally {
    btn.textContent = '应用色卡';
    btn.disabled = !document.getElementById('paletteEnabled').checked ||
                   !EditorState.pixels || currentPalette.length === 0;
  }
}
```

**G. 绑定 `applyPaletteBtn` 和关闭按钮（在 DOMContentLoaded 中）：**
```javascript
document.getElementById('applyPaletteBtn').addEventListener('click', applyPalette);
document.getElementById('pal-result-close').addEventListener('click', () => {
  document.getElementById('palette-result-panel').style.display = 'none';
});
```

**注意：应用色卡不调用 pushHistory()，不修改 EditorState.pixels。**
  </action>
  <verify>
1. 在调色盘中选一个颜色 → 执行 `EditorState.foregroundColor` 确认颜色，在色卡中找一个匹配的色块 → 该色块出现发光效果（白色内圈 + 彩色外晕）
2. 点击「应用色卡」→ Network 可见 POST /api/apply-palette → 画布右侧出现对比图（原图 + 色卡映射）
3. 验证 EditorState.pixels 未被修改：应用前后执行 `EditorState.pixels[0]` 返回相同值
4. 执行 Cmd+Z 撤销 → 历史步骤数不变（应用色卡不计入历史）
5. 点击「关闭结果」→ 结果面板消失
  </verify>
  <done>highlightMatchingSwatches() 在每次 syncColorUI() 后执行；色块 glow 效果正确显示且不被裁剪；applyPalette() 非破坏性预览；结果对比面板含精准版/放大版下载链接。</done>
</task>

<task type="auto">
  <name>Task 2: 从 web_ui.html 删除色卡面板代码</name>
  <files>web_ui.html</files>
  <action>
从 web_ui.html 中完整删除色卡限制面板，具体删除范围：

**HTML 删除（约 lines 354-488 + 522-540 + 542-558）：**
- 删除 `<!-- ── Palette section ── -->` 注释及其下的整个 `.palette-section` div（从 `<div class="palette-section">` 到对应的 `</div><!-- /palette-section -->`）
- 删除 `<div class="img-card" id="paletteResultCard">` 结果卡片 div（及其子内容）
- 删除 `<div class="color-popup" id="colorPopup">` 颜色编辑弹窗 div

**CSS 删除（约 lines 108-230）：**
删除以下 CSS 块（通过注释边界定位）：
- `/* ── Palette section ── */` 段落（.palette-section 到 .color-popup 的全部 palette 相关样式）
- `.palette-strip`, `.palette-strip-dot` 样式
- 保留与其他功能共用的通用类（.btn, .btn-secondary 等），只删除 palette 专属的类

**JS 删除（约 lines 562-1139）：**
- 删除 `let currentPalette`, `editingSwatchIdx`, `selectedPaletteKey`, `currentPaletteMeta` 变量声明
- 删除所有 palette 相关函数：`setCurrentPalette`, `renderSwatches`, `deleteSwatch`, `openColorEditor`, `closeColorEditor`, `syncFromRgb`, `syncFromHex`, `syncFromPicker`（popup editor 内部的）, `savePalette`, `loadSavedPalette`, `deleteSavedPalette`, `populateSavedPalettes`, `exportPalette`, `applyPalette`（web_ui 版本）
- 删除 DOMContentLoaded 中与色卡面板相关的所有事件绑定

**不添加任何替代文字或跳转链接**（CONTEXT.md 锁定决策：直接删除，不留引导）。

删除后验证 web_ui.html 基本功能（图像上传、像素化处理）不受影响。
  </action>
  <verify>
1. 打开 http://localhost:5010 → 页面正常加载，无 JS 控制台错误
2. 上传一张图片 → 像素化处理功能正常（主功能不受影响）
3. 页面上不再有色卡限制相关的 HTML 元素（通过 browser DevTools 确认无 #paletteSection / #paletteHeader 等元素）
4. 删除后的 web_ui.html 文件大小明显减小（约减少 700-800 行）
  </verify>
  <done>web_ui.html 中色卡面板 HTML、CSS、JS 全部移除；web_ui.html 核心像素化处理功能完整保留；页面无 JS 报错。</done>
</task>

</tasks>

<verification>
验证 Plan 02 完整性：
- `highlightMatchingSwatches()` 函数存在于 editor.html script 中
- `syncColorUI()` 末尾（_syncLock = false 后）调用 `highlightMatchingSwatches()`
- 色块 glow 正常显示（非 hidden/clipped）
- applyPalette() 调用 /api/apply-palette 并显示对比图
- EditorState.pixels 在应用色卡前后不变
- web_ui.html 中无 paletteSection / swatchesGrid 等 ID
</verification>

<success_criteria>
- PAL-02 实现：调色盘颜色与色块精确 RGB 匹配时，色块出现 box-shadow glow 效果
- 应用色卡结果：非破坏性预览，原图 + 映射结果并列，含精准版/放大版下载
- web_ui.html 色卡代码已清除，主功能正常
</success_criteria>

<output>
完成后创建 `.planning/phases/04-palette-panel/04-02-SUMMARY.md`
</output>
