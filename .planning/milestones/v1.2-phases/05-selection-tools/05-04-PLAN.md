---
phase: 05-selection-tools
plan: "04"
type: execute
wave: 4
depends_on: [05-03]
files_modified: []
autonomous: false

requirements: [SEL-01, SEL-02, SEL-03, SEL-04, SEL-05]

must_haves:
  truths:
    - "Dragging Rectangle Marquee produces grid-snapped selection with animated marching-ants"
    - "Magic Wand click produces irregular pixel-mask selection with matching marching-ants outline"
    - "Tolerance and Contiguous options change the Wand selection extent visibly"
    - "Pencil strokes clip to selection boundary — no paint leaks outside"
    - "Eraser strokes clip to selection boundary"
    - "Bucket fill clips to selection boundary"
    - "Deselect (Cmd+D) removes selection and stops marching-ants animation"
    - "Inverse (Shift+Cmd+I) inverts the selection mask"
    - "Delete key clears selected pixels to transparent"
    - "All selection operations are undoable"
  artifacts:
    - path: "editor.html"
      provides: "Fully working Phase 5 selection tools"
  key_links:
    - from: "Phase 5 success criteria"
      to: "Visual browser verification"
      via: "Human confirms all 4 success criteria from ROADMAP.md"
---

<objective>
Visual verification of Phase 5 complete implementation across all success criteria.

Purpose: All implementation is done in Plans 01-03. This checkpoint confirms the selection tools work correctly end-to-end in a real browser before closing Phase 5.
Output: Human confirmation that Phase 5 success criteria are all met.
</objective>

<execution_context>
@/Users/calling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/calling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-selection-tools/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 5 visual verification — all selection tools in browser</name>
  <action>
This is a human verification checkpoint. No code changes. Confirm the complete Phase 5 implementation works in the browser by following the verification steps below.
  </action>
  <verify>
Flask must be running: `python3 /Users/calling/perfectPixel_ver1.1/web_app.py`
Open: http://localhost:5010/editor

**Test 1 — Rectangle Marquee (SEL-01, SEL-02):**
1. Press M to activate Marquee
2. Drag across a portion of the canvas
3. Confirm: dashed rect preview appears during drag
4. Release: marching-ants animated border appears on the committed selection
5. If gridW/gridH > 0: confirm selection snaps to grid corners (`EditorState.gridW` in console)
6. Click without dragging: confirm selection disappears (zero-size deselect)

**Test 2 — Magic Wand (SEL-03, SEL-04):**
1. Press W to activate Wand; top bar shows "Tolerance" input and "Contiguous" checkbox
2. Click a pixel region — irregular marching-ants appear following the color boundary
3. Set Tolerance to 50, click again — larger region selected
4. Uncheck Contiguous, click — all matching-color pixels across entire canvas selected

**Test 3 — Deselect + Inverse (SEL-05):**
1. With selection active: Deselect and Inverse buttons visible in top bar
2. Press Cmd+D: ants stop, canvas clears, buttons hide
3. New selection → click Inverse: ants wrap the inverse region
4. No selection → Inverse: entire canvas selected

**Test 4 — Tool clipping:**
1. Create a marquee selection covering ~25% of canvas
2. Press B (Pencil), draw across boundary — stroke stops at edge, no leaking
3. Press E (Eraser), erase across boundary — stops at edge
4. Press G (Bucket), click inside — fill stops at boundary

**Test 5 — Delete operations:**
1. Create a selection
2. Press Delete — pixels become transparent (checkerboard visible); Cmd+Z restores
3. Press Option+Delete — pixels fill with foreground color; Cmd+Z restores

**Test 6 — Selection persistence + Shift-union:**
1. Create selection, switch B/E/G/M/W: selection stays active
2. Shift+drag new marquee: both regions selected (ants cover both areas)
  </verify>
  <done>Human types "approved" after confirming all tests pass</done>
</task>

</tasks>

<verification>
Human verification of all Phase 5 success criteria from ROADMAP.md:
1. Dragging Rectangle Marquee (M) produces grid-snapped selection with animated marching-ants — VERIFIED
2. Magic Wand (W) selects similar-color region; Tolerance and Contiguous change extent — VERIFIED
3. Pencil and Eraser strokes clip to selected region — VERIFIED
4. Deselect (Cmd+D) and Inverse (Shift+Cmd+I) work; buttons appear in top bar — VERIFIED
</verification>

<success_criteria>
All 4 ROADMAP Phase 5 success criteria confirmed by human:
1. Rectangle Marquee with grid-snap + marching-ants
2. Magic Wand with Tolerance + Contiguous
3. Tool clipping (Pencil + Eraser + Bucket)
4. Deselect + Inverse top-bar buttons and shortcuts
</success_criteria>

<output>
After completion, create `.planning/phases/05-selection-tools/05-04-SUMMARY.md`
</output>
