---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web_app.py
  - editor.html
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "http://localhost:5010/editor 能在浏览器中打开，返回 editor.html 页面"
    - "页面显示 4 面板布局：顶部栏 / 左侧面板 / 中央画布区 / 右侧工具栏"
    - "左侧面板包含可折叠的占位色卡区和固定底部的像素检查器脚手架"
    - "右侧面板显示 6 个禁用工具槽（Marquee/Wand/Move/Pencil/Bucket/Eraser）"
    - "顶部栏显示完整 chrome：Zoom 控件（激活）、Undo/Redo 占位（禁用）"
    - "整体深色主题与 web_ui.html 保持一致（CSS 变量复用）"
  artifacts:
    - path: "editor.html"
      provides: "单文件编辑器页面（内联 CSS + JS 骨架）"
      min_lines: 150
    - path: "web_app.py"
      provides: "新增 /editor 和 /output.png 路由"
      contains: "route(\"/editor\")"
  key_links:
    - from: "web_app.py"
      to: "editor.html"
      via: "@app.route('/editor') → send_file('editor.html')"
      pattern: "route.*editor"
    - from: "editor.html"
      to: "web_app.py"
      via: "fetch('/output.png') 在 initEditor() 调用"
      pattern: "fetch.*output.png"
---

<objective>
搭建 editor.html 的 HTML/CSS 骨架，并在 web_app.py 中注册两条新路由（/editor 和 /output.png），使编辑器页面可以在浏览器中访问，并呈现正确的 4 面板深色主题布局。

Purpose: Phase 1 的基础结构必须在 Phase 2/3 之前完全就绪。布局骨架和路由是所有后续工作的前提条件。
Output: web_app.py（+2 路由）、editor.html（完整 CSS 布局骨架 + 右侧工具占位 + 左侧面板结构 + 顶部栏 chrome + JS 骨架变量）
</objective>

<execution_context>
@/Users/calling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/calling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- web_app.py 现有模式（executor 必须匹配） -->

现有路由模式（web_app.py 第 346-348 行）：
```python
@app.route("/")
def index():
    return send_file(os.path.join(os.path.dirname(__file__), "web_ui.html"))
```

main block（文件末尾）：
```python
if __name__ == "__main__":
    print("Perfect Pixel Web UI running at http://localhost:5010")
    app.run(host="0.0.0.0", port=5010, debug=False)
```

CSS 变量（从 web_ui.html 直接复制，不要重新定义）：
```css
:root {
  --bg: #0f0f13;
  --surface: #1a1a22;
  --surface2: #22222e;
  --border: #2e2e3e;
  --accent: #7c6af7;
  --accent-hover: #9080ff;
  --text: #e8e6f0;
  --text-muted: #7a7890;
  --success: #4ade80;
  --error: #f87171;
  --warning: #fbbf24;
  --radius: 10px;
}
```

按钮样式（从 web_ui.html 直接复制）：
```css
.btn { padding:8px 12px; border-radius:7px; border:none; font-size:13px; font-weight:600; cursor:pointer; transition:background .2s,opacity .2s; white-space:nowrap; }
.btn:disabled { opacity:.4; cursor:not-allowed; }
.btn-primary { background:var(--accent); color:#fff; }
.btn-primary:hover:not(:disabled) { background:var(--accent-hover); }
.btn-secondary { background:var(--surface2); color:var(--text); border:1px solid var(--border); }
.btn-secondary:hover:not(:disabled) { border-color:var(--accent); }
.btn-ghost { background:transparent; color:var(--text-muted); border:1px solid var(--border); padding:5px 9px; font-size:12px; }
.btn-ghost:hover { color:var(--text); border-color:var(--accent); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 web_app.py 中注册 /editor 和 /output.png 路由</name>
  <files>web_app.py</files>
  <action>
在 `web_app.py` 的 Flask 路由区（`# ── Flask routes ──` 注释之后，`if __name__ == "__main__"` 之前）新增两条路由，紧接在现有 `index()` 路由之后：

```python
@app.route("/editor")
def editor():
    return send_file(os.path.join(os.path.dirname(__file__), "editor.html"))

@app.route("/output.png")
def output_png():
    return send_file(os.path.join(os.path.dirname(__file__), "output.png"))
```

约束：
- 不修改任何现有路由或函数
- `send_file` 已在 import 行导入，无需新增 import
- `os` 已导入，无需新增 import
- 路由函数名使用 `editor` 和 `output_png`（下划线，非连字符）
  </action>
  <verify>
    <automated>cd /Users/calling/perfectPixel_ver1.1 && python3 -c "import ast, sys; ast.parse(open('web_app.py').read()); print('syntax OK')" && grep -c "route.*editor" web_app.py | grep -q "1" && echo "route present"</automated>
  </verify>
  <done>web_app.py 语法正确，包含 `/editor` 路由和 `/output.png` 路由各一条</done>
</task>

<task type="auto">
  <name>Task 2: 创建 editor.html 布局骨架（CSS + HTML 结构 + JS 变量声明）</name>
  <files>editor.html</files>
  <action>
创建 `/Users/calling/perfectPixel_ver1.1/editor.html`，单文件内联 CSS + JS，完整实现以下内容：

**HTML 结构（4 面板）：**
```
<body>
  <header id="top-bar">            ← 顶部栏
  <div id="layout">                ← CSS Grid 容器
    <div id="left-panel">          ← 左侧面板
      <div id="left-scroll">       ← 可滚动上方区域（色卡占位）
      <div id="pixel-inspector">   ← 固定底部像素检查器
    <div id="canvas-area">         ← 中央画布区（Phase 02 填充）
    <div id="right-panel">         ← 右侧工具条
```

**CSS 要求（严格遵守）：**
- `:root` CSS 变量：从 RESEARCH.md 的 "CSS Variables" 代码块**逐字复制**（不要重新设计颜色）
- `.btn`, `.btn-primary`, `.btn-secondary`, `.btn-ghost` 样式：从 RESEARCH.md 的 "Button Styles" 代码块**逐字复制**
- `body`：`display:flex; flex-direction:column; height:100vh; overflow:hidden; background:var(--bg); color:var(--text); font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif; font-size:14px; margin:0`
- `#top-bar`：`flex-shrink:0; height:44px; padding:0 16px; display:flex; align-items:center; gap:12px; border-bottom:1px solid var(--border); background:var(--surface)`
- `#layout`：`flex:1; display:grid; grid-template-columns:280px 1fr 48px; min-height:0; overflow:hidden`
- `#left-panel`：`border-right:1px solid var(--border); display:flex; flex-direction:column; overflow:hidden; background:var(--surface)`
- `#left-scroll`：`flex:1; overflow-y:auto; padding:12px; display:flex; flex-direction:column; gap:12px`
- `#pixel-inspector`：`flex-shrink:0; border-top:1px solid var(--border); padding:10px 12px; background:var(--surface); font-size:12px`
- `#canvas-area`：`overflow:auto; position:relative; background:var(--bg)`
- `#right-panel`：`border-left:1px solid var(--border); display:flex; flex-direction:column; align-items:center; padding:8px 0; gap:4px; background:var(--surface)`
- `.tool-btn`：`width:36px; height:36px; border:1px solid var(--border); border-radius:6px; background:var(--surface2); color:var(--text-muted); cursor:pointer; font-size:16px; display:flex; align-items:center; justify-content:center`
- `.tool-btn:disabled`：`opacity:.3; cursor:not-allowed`
- `.tool-sep`：`width:28px; height:1px; background:var(--border); margin:2px 0`

**顶部栏 HTML 内容（使用 RESEARCH.md 的 "Top Bar Chrome" 模式）：**
- 左侧：`<span>PerfectPixel Editor</span>`（font-weight:700, font-size:15px）
- 中间：`<span style="flex:1"></span>` spacer
- 右侧 Zoom 控件（**激活状态**）：`-` 按钮（id="btn-zoom-out"）、zoom 数值显示（id="zoom-display"，初始文本 "4x"）、`+` 按钮（id="btn-zoom-in"）
- 右侧占位（**禁用状态**）：Undo 按钮（disabled）、Redo 按钮（disabled）

**左侧面板 HTML 内容：**
- `#left-scroll` 内放置一个折叠式占位卡片，结构：
  ```html
  <div class="panel-card">
    <div class="panel-header" style="cursor:pointer; display:flex; justify-content:space-between; align-items:center; padding:8px; border-bottom:1px solid var(--border)">
      <span style="font-weight:600; font-size:13px;">色卡限制</span>
      <span style="font-size:11px; color:var(--text-muted)">[Phase 4]</span>
    </div>
  </div>
  ```
- `#pixel-inspector` 内容：
  ```html
  <div style="font-size:11px; color:var(--text-muted); margin-bottom:4px;">像素检查器</div>
  <div style="display:flex; align-items:center; gap:8px;">
    <div id="insp-swatch" style="width:20px;height:20px;border:1px solid var(--border);border-radius:3px;background:#000;flex-shrink:0"></div>
    <div style="display:flex; flex-direction:column; gap:2px; font-size:11px; font-variant-numeric:tabular-nums;">
      <span>X: <span id="insp-x">—</span>  Y: <span id="insp-y">—</span></span>
      <span>R: <span id="insp-r">—</span>  G: <span id="insp-g">—</span>  B: <span id="insp-b">—</span>  A: <span id="insp-a">—</span></span>
    </div>
  </div>
  ```

**右侧工具条 HTML 内容（使用 RESEARCH.md 的 "Tool Placeholder" 模式）：**
按以下顺序放置，全部 disabled：
- Marquee 按钮（title="Marquee (M)"）、Wand 按钮（title="Wand (W)"）
- `.tool-sep` 分隔线
- Move 按钮（title="Move (V)"）
- `.tool-sep` 分隔线
- Pencil 按钮（title="Pencil (B)"）、Bucket 按钮（title="Bucket (G)"）
- `.tool-sep` 分隔线
- Eraser 按钮（title="Eraser (E)"）

**`#canvas-area` 内容（Phase 02 的占位符）：**
放置一个 `<div id="zoom-outer">` 包裹 `<div id="zoom-container">`，内容为空（三层 canvas 由 Plan 02 的 JS 填充）：
```html
<div id="zoom-outer" style="min-width:100%;min-height:100%;display:flex;align-items:center;justify-content:center;padding:24px;box-sizing:border-box;">
  <div id="zoom-container" style="position:relative;transform-origin:top left;"></div>
</div>
```

**JS 骨架（`<script>` 标签，Plan 02 继续填充）：**
只声明 `EditorState` 对象（从 RESEARCH.md 的 "Pattern 4" 代码块复制完整定义）和以下空函数签名（无实现，Phase 02 填充）：
```javascript
// EditorState — 完整定义（Pattern 4 from RESEARCH.md）
const EditorState = { ... }; // 完整对象，见 RESEARCH.md Pattern 4

// 函数签名占位（Phase 02 实现）
function initCanvases(width, height) {}
function flushPixels() {}
function viewportToCanvas(clientX, clientY) { return [0, 0]; }
function getPixel(x, y) { return [0, 0, 0, 0]; }
function applyZoom(newZoom, pivotX, pivotY) {}
async function loadPlaceholderImage() {}

// DOM ready
document.addEventListener('DOMContentLoaded', () => {
  loadPlaceholderImage();
});
```

约束：
- **不使用 npm 或任何外部库**，纯内联 CSS + JS
- **不引用外部 CSS/JS 文件**，一切内联
- `<meta charset="UTF-8">` 和 `<meta name="viewport" content="width=device-width, initial-scale=1">` 必须存在
- `<title>PerfectPixel Editor</title>` 必须存在
- `.panel-card` 基本样式：`border:1px solid var(--border); border-radius:var(--radius); overflow:hidden; background:var(--surface2)`
  </action>
  <verify>
    <automated>cd /Users/calling/perfectPixel_ver1.1 && python3 -c "
content = open('editor.html').read()
checks = [
  '--accent: #7c6af7' in content,
  'id=\"top-bar\"' in content,
  'id=\"layout\"' in content,
  'id=\"left-panel\"' in content,
  'id=\"canvas-area\"' in content,
  'id=\"right-panel\"' in content,
  'id=\"pixel-inspector\"' in content,
  'id=\"zoom-container\"' in content,
  'btn-zoom-out' in content,
  'zoom-display' in content,
  'EditorState' in content,
  '280px 1fr 48px' in content,
]
failed = [i for i, c in enumerate(checks) if not c]
if failed: print('FAIL checks:', failed)
else: print('ALL PASS')
"</automated>
  </verify>
  <done>editor.html 存在，包含正确的 4 面板 CSS 布局、深色主题变量、顶部栏 chrome、左侧面板结构（含像素检查器 DOM 节点）、右侧工具占位、EditorState 声明</done>
</task>

</tasks>

<verification>
1. 在运行中的 Flask 服务器（端口 5010）上访问 http://localhost:5010/editor，返回 200 且显示编辑器页面
2. 页面可见 4 面板布局，深色主题与 web_ui.html 风格一致
3. 顶部栏的 Zoom +/- 按钮可点击（不报错），Undo/Redo 显示为禁用状态
4. 左侧面板底部可见像素检查器区域（DOM 节点存在，值显示为 "—"）
5. 右侧工具条 6 个图标按钮全部呈现（禁用状态）
6. 浏览器控制台无 JS 错误（骨架函数均为空实现，不应报错）
</verification>

<success_criteria>
- /editor 路由注册成功，http://localhost:5010/editor 返回 editor.html
- /output.png 路由注册成功，http://localhost:5010/output.png 返回图片
- editor.html 的 4 面板布局在浏览器中正确渲染
- 深色主题 CSS 变量与 web_ui.html 一致（直接复用，非重写）
- 页面无 JS 报错（骨架函数均为空占位）
</success_criteria>

<output>
完成后，创建 `.planning/phases/01-foundation/01-01-SUMMARY.md`，记录：
- 创建/修改了哪些文件
- Flask 路由地址
- editor.html 的 HTML 结构和 CSS 变量来源
- 已知的 JS 骨架（空函数，Phase 02 填充）
- 任何与计划偏差的说明
</output>
