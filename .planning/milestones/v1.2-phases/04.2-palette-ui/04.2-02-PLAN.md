---
phase: 04.2-palette-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 04.2-01
files_modified:
  - editor.html
autonomous: false
requirements:
  - PAL-UI

must_haves:
  truths:
    - "点击 + 色块把当前前景色加入色卡，renderSwatches() 重渲染后 + 号始终在末尾"
    - "下拉 ▼ 按钮展开已保存色卡列表，点击某条目自动加载该色卡并关闭下拉"
    - "下拉列表底部有「📁 从本地上传」选项，点击后弹出文件选择器"
    - "导出▼ 展开三个选项（.act/.gpl/.pal），点击对应选项触发文件下载"
    - "应用色卡按钮在无图像或色卡为空时禁用；有图像且色卡非空时启用（不再依赖 paletteEnabled 开关）"
    - "applyPalette() 正确读取 #mappingModeSelect 的值作为映射模式"
    - "所有旧的 JS 绑定（paletteEnabled change、tab-btn、addColorBtn、loadPaletteBtn、deletePaletteBtn、savedPalettesTrigger/旧 dropdown）已删除"
  artifacts:
    - path: "editor.html"
      provides: "完整 JS 接线：renderSwatches()+add-swatch、refreshSavedDropdown()、applyPalette()、setCurrentPalette() gate、导出下拉、palDropBtn 触发"
      contains: "add-swatch, palDropBtn, savedPaletteOptions, mappingModeSelect, exportDropBtn, exportMenu"
  key_links:
    - from: "renderSwatches()"
      to: ".add-swatch div"
      via: "grid.appendChild(addSw) at end of function"
      pattern: "add-swatch"
    - from: "refreshSavedDropdown()"
      to: "setCurrentPalette(palette)"
      via: "div click handler inside forEach"
      pattern: "setCurrentPalette"
    - from: "applyPalette()"
      to: "#mappingModeSelect"
      via: "document.getElementById('mappingModeSelect').value"
      pattern: "mappingModeSelect"
    - from: "setCurrentPalette()"
      to: "applyBtn.disabled"
      via: "!EditorState.pixels || currentPalette.length === 0"
      pattern: "applyBtn.disabled"
---

<objective>
完成 Plan 01 后的全部 JS 接线工作，使新 HTML 骨架与现有逻辑正确绑定，同时清理所有指向已删除 DOM 元素的旧绑定。

Purpose: Plan 01 只改了 HTML/CSS，JS 仍指向旧 DOM id（paletteEnabled、tab-btn、addColorBtn 等）。本 Plan 修复全部断开的绑定，注入新功能（+ 色块、选中即加载、导出下拉），并通过人工验证确认完整功能。
Output: editor.html JS 完整、无 console 错误，面板所有交互正常工作。
</objective>

<execution_context>
@/Users/calling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/calling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.2-palette-ui/04.2-CONTEXT.md
@.planning/phases/04.2-palette-ui/04.2-RESEARCH.md
@.planning/phases/04.2-palette-ui/04.2-01-SUMMARY.md

<interfaces>
<!-- JS 改动清单（来自 RESEARCH.md 示例 2，以函数名定位为主） -->

① setCurrentPalette() 内 applyBtn gate：
   原：const palEnabled = document.getElementById('paletteEnabled');
       if (applyBtn && palEnabled) { applyBtn.disabled = !palEnabled.checked || ... }
   改：if (applyBtn) { applyBtn.disabled = !EditorState.pixels || currentPalette.length === 0; }

② applyPalette() 内映射模式读取：
   原：const mode = (document.querySelector('input[name="mappingMode"]:checked') || {value: 'vector'}).value;
   改：const modeEl = document.getElementById('mappingModeSelect');
       const mode = modeEl ? modeEl.value : 'vector';

③ renderSwatches() 末尾追加 .add-swatch（在 highlightMatchingSwatches() 调用之前）：
   const addSw = document.createElement('div');
   addSw.className = 'add-swatch';
   addSw.title = '添加当前前景色到色卡';
   addSw.textContent = '+';
   addSw.addEventListener('click', () => {
     const [r, g, b] = EditorState.foregroundColor;
     currentPalette.push([r, g, b]);
     EditorState.palette = currentPalette.slice();
     renderSwatches();
   });
   grid.appendChild(addSw);

④ refreshSavedDropdown() 双处修改：
   a. forEach 内 div.click 处理器改为「选中即加载」
   b. 末尾追加上传选项（📁 从本地上传）

⑤ palDropBtn 触发逻辑（新增，替代 savedPalettesTriggerEl）：
   const palDropBtnEl = document.getElementById('palDropBtn');
   const savedPaletteOptionsEl = document.getElementById('savedPaletteOptions');
   if (palDropBtnEl && savedPaletteOptionsEl) {
     palDropBtnEl.addEventListener('click', e => {
       e.stopPropagation();
       refreshSavedDropdown();
       const isOpen = savedPaletteOptionsEl.style.display !== 'none';
       savedPaletteOptionsEl.style.display = isOpen ? 'none' : 'block';
     });
     document.addEventListener('click', e => {
       if (!e.target.closest('#savedPaletteOptions') && e.target !== palDropBtnEl)
         savedPaletteOptionsEl.style.display = 'none';
     });
   }

⑥ 导出下拉按钮（新增 exportDropBtn 开关逻辑）：
   const exportDropBtnEl = document.getElementById('exportDropBtn');
   const exportMenuEl = document.getElementById('exportMenu');
   if (exportDropBtnEl && exportMenuEl) {
     exportDropBtnEl.addEventListener('click', e => {
       e.stopPropagation();
       exportMenuEl.style.display = exportMenuEl.style.display === 'none' ? 'block' : 'none';
     });
     document.addEventListener('click', () => { exportMenuEl.style.display = 'none'; });
   }
   // exportAct/Gpl/Pal id 不变，原有绑定无需修改

⑦ 需要删除的旧 JS 块（定位方式：搜索关键字符串）：
   - paletteEnabled change 监听块（搜索 "paletteEnabled"，删除整个 if(palEnabledEl) 事件绑定块）
   - tab-btn querySelectorAll 切换块（搜索 "tab-btn"，删除 tab 切换逻辑）
   - palUploadZone drag/drop 绑定（搜索 "palUploadZone"，删除 dragover/drop 事件绑定；palFileInput.change 绑定保留）
   - addColorBtnEl 绑定（搜索 "addColorBtnEl" 或 "addColorBtn"，删除整个绑定块）
   - savedPalettesTriggerEl 绑定（搜索 "savedPalettesTriggerEl"，删除旧触发器绑定；新 palDropBtn 逻辑在步骤⑤中添加）
   - loadPaletteBtnEl 绑定（搜索 "loadPaletteBtnEl"，删除整个块；逻辑已移入 refreshSavedDropdown click 回调）
   - deletePaletteBtnEl 绑定（搜索 "deletePaletteBtnEl"，删除整个块；D4 决策：删除功能不做）

⑧ uploadPaletteFile 函数内 #palFilename 引用：
   uploadPaletteFile() 成功回调中若有 document.getElementById('palFilename') 引用，
   改为仅用 palShowStatus() 显示文件名，不写入已删除的 #palFilename 元素
   （原代码已有 null 检查，不会报错，但最好改为 palShowStatus 主动展示）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: JS 绑定清理 + gate 修改 + applyPalette 模式读取</name>
  <files>editor.html</files>
  <action>
在 editor.html 的 `<script>` 部分执行以下修改（以函数名/字符串搜索定位，行号仅供参考）：

**A. 修改 setCurrentPalette() 内的 applyBtn.disabled gate**

搜索 `palEnabled.checked` 或 `paletteEnabled` 在 setCurrentPalette 函数内的出现。将：
```javascript
const palEnabled = document.getElementById('paletteEnabled');
if (applyBtn && palEnabled) {
  applyBtn.disabled = !palEnabled.checked || !EditorState.pixels || currentPalette.length === 0;
}
```
改为：
```javascript
if (applyBtn) {
  applyBtn.disabled = !EditorState.pixels || currentPalette.length === 0;
}
```

**B. 修改 applyPalette() 内的映射模式读取**

搜索 `input[name="mappingMode"]:checked`，将：
```javascript
const mode = (document.querySelector('input[name="mappingMode"]:checked') || {value: 'vector'}).value;
```
改为：
```javascript
const modeEl = document.getElementById('mappingModeSelect');
const mode = modeEl ? modeEl.value : 'vector';
```

**C. 删除旧 JS 绑定块（逐一搜索并删除）**

1. 搜索 `paletteEnabled`（change 事件监听器），定位整个监听块（约 15 行），删除
2. 搜索 `tab-btn`（tab 切换 querySelectorAll 块），定位整个块（约 9 行），删除
3. 搜索 `palUploadZone`，定位 dragover/dragleave/drop 事件绑定（约 10 行），删除；保留 palFileInput.change 绑定不变
4. 搜索 `addColorBtnEl` 或 `addColorBtn`（addColorBtn 绑定块），定位并删除整个块
5. 搜索 `savedPalettesTriggerEl`（旧触发器绑定块），定位并删除；此步骤与步骤 D 的新 palDropBtn 逻辑配合
6. 搜索 `loadPaletteBtnEl`（加载按钮绑定），定位并删除整个块
7. 搜索 `deletePaletteBtnEl`（删除按钮绑定），定位并删除整个块

每次删除后确认其他代码结构完整（大括号配对）。

**D. 新增 palDropBtn 触发逻辑**

在清理了 savedPalettesTriggerEl 旧绑定的位置（或在 savePaletteBtn 绑定附近）新增：

```javascript
const palDropBtnEl = document.getElementById('palDropBtn');
const savedPaletteOptionsEl = document.getElementById('savedPaletteOptions');
if (palDropBtnEl && savedPaletteOptionsEl) {
  palDropBtnEl.addEventListener('click', e => {
    e.stopPropagation();
    refreshSavedDropdown();
    const isOpen = savedPaletteOptionsEl.style.display !== 'none';
    savedPaletteOptionsEl.style.display = isOpen ? 'none' : 'block';
  });
  document.addEventListener('click', e => {
    if (!e.target.closest('#savedPaletteOptions') && e.target !== palDropBtnEl)
      savedPaletteOptionsEl.style.display = 'none';
  });
}
```

**E. 新增导出下拉开关逻辑**

在 exportAct/Gpl/Pal 绑定之前（或之后）新增：

```javascript
const exportDropBtnEl = document.getElementById('exportDropBtn');
const exportMenuEl = document.getElementById('exportMenu');
if (exportDropBtnEl && exportMenuEl) {
  exportDropBtnEl.addEventListener('click', e => {
    e.stopPropagation();
    exportMenuEl.style.display = exportMenuEl.style.display === 'none' ? 'block' : 'none';
  });
  // 全局点击关闭导出菜单（在已有的 document.addEventListener('click') 中追加，或新增）
  // 若已有全局 click 关闭逻辑可合并；若没有则新增：
  document.addEventListener('click', () => { exportMenuEl.style.display = 'none'; });
}
```

注意：exportAct/Gpl/Pal 三个按钮的 id 不变，其原有绑定保持不变，不需要修改。

**F. 修复 uploadPaletteFile 中的 #palFilename 引用**

搜索 `palFilename`，若存在 `getElementById('palFilename')` 后写入 `.textContent`，将该行改为调用 `palShowStatus()`，例如：
```javascript
// 原（若有）：
const palFilenameEl = document.getElementById('palFilename');
if (palFilenameEl) palFilenameEl.textContent = file.name;
// 改为（或保留原有 null-check 不变也可接受，因 palFilename 为 null 时 if 块静默跳过）：
palShowStatus(`已上传：${file.name}`, 'success');
```
若原代码已有 `if (palFilenameEl)` null 检查，可保留不变（不会报错）。

**不要做的事：**
- 不要修改 renderSwatches()（由 Task 2 处理）
- 不要修改 refreshSavedDropdown()（由 Task 2 处理）
- 不要修改 applyPalette() 内除映射模式读取以外的任何逻辑
- 不要删除 palFileInput.change 绑定（上传功能仍需要它）
  </action>
  <verify>
    <automated>python3 -c "
content = open('/Users/calling/perfectPixel_ver1.1/editor.html').read()
checks = [
  ('palEnabled.checked' not in content, 'applyBtn gate no longer checks paletteEnabled'),
  ('mappingModeSelect' in content, 'mappingModeSelect used in applyPalette'),
  ('input[name=\"mappingMode\"]:checked' not in content, 'old radio querySelector removed'),
  ('palDropBtn' in content, 'palDropBtn wired'),
  ('exportDropBtn' in content, 'exportDropBtn wired'),
]
failures = [msg for ok, msg in checks if not ok]
if failures:
    print('FAIL:', failures)
    exit(1)
print('ALL JS BINDING CHECKS PASSED')
"
    </automated>
  </verify>
  <done>setCurrentPalette gate 已去掉 paletteEnabled 依赖；applyPalette 读取 mappingModeSelect；所有旧绑定已删除；palDropBtn 和 exportDropBtn 新逻辑已添加。</done>
</task>

<task type="auto">
  <name>Task 2: 修改 renderSwatches() 和 refreshSavedDropdown() 函数体</name>
  <files>editor.html</files>
  <action>
**A. 修改 renderSwatches() — 末尾追加 .add-swatch**

定位 `renderSwatches` 函数（约第 1619–1652 行）。在函数末尾、`highlightMatchingSwatches()` 调用之前（若有），追加以下代码：

```javascript
// 追加 + 色块（.add-swatch）— Phase 4.2
const addSwEl = document.createElement('div');
addSwEl.className = 'add-swatch';
addSwEl.title = '添加当前前景色到色卡';
addSwEl.textContent = '+';
addSwEl.addEventListener('click', () => {
  const [r, g, b] = EditorState.foregroundColor;
  currentPalette.push([r, g, b]);
  EditorState.palette = currentPalette.slice();
  renderSwatches();
});
grid.appendChild(addSwEl);
```

关键约束：
- `.add-swatch` className 不包含 `.swatch`，确保 `highlightMatchingSwatches()` 的 `querySelectorAll('.swatch')` 不会选中它（RESEARCH.md Pitfall 4）
- `addSwEl` 变量名带 `El` 后缀，避免与任何外层同名变量冲突
- 追加在 `grid.appendChild` 所有 swatch 循环结束之后、函数结束之前

**B. 修改 refreshSavedDropdown() — 两处改动**

定位 `refreshSavedDropdown` 函数（约第 1748–1792 行）。

**B1. 将 forEach 内的 div.click 改为「选中即加载」：**

找到 forEach 内对 `div` 设置 click 事件的部分。将原来仅更新 `selectedPaletteKey` 和触发文字的逻辑，改为：

```javascript
div.addEventListener('click', () => {
  selectedPaletteKey = name;
  const paletteNameInputEl = document.getElementById('paletteName');
  if (paletteNameInputEl) paletteNameInputEl.value = name;
  setCurrentPalette(palette);
  const savedPaletteOptionsEl = document.getElementById('savedPaletteOptions');
  if (savedPaletteOptionsEl) savedPaletteOptionsEl.style.display = 'none';
  palShowStatus(`已加载色卡「${name}」：${palette.length} 色`, 'success');
});
```

**B2. 末尾追加「📁 从本地上传」选项：**

在 `keys.forEach` 循环结束之后、函数 return 之前，追加：

```javascript
// 底部上传选项
const uploadOptEl = document.createElement('div');
uploadOptEl.className = 'custom-option';
uploadOptEl.style.borderTop = '1px solid var(--border)';
uploadOptEl.innerHTML = '<span style="font-size:12px; color:var(--text-muted);">📁 从本地上传</span>';
uploadOptEl.addEventListener('click', () => {
  const savedPaletteOptionsEl = document.getElementById('savedPaletteOptions');
  if (savedPaletteOptionsEl) savedPaletteOptionsEl.style.display = 'none';
  const palFileInputEl = document.getElementById('palFileInput');
  if (palFileInputEl) palFileInputEl.click();
});
opts.appendChild(uploadOptEl);
```

注意：RESEARCH.md Pitfall 2 提醒 `selectedPaletteKey` 必须在 click 第一行同步更新，已在 B1 中第一行赋值。

**不要修改的部分：**
- refreshSavedDropdown 中获取 savedPalettes、构建 opts div 的逻辑
- renderSwatches 中现有 swatch 渲染循环（双击打开颜色编辑弹窗等逻辑不变）
- 任何其他函数
  </action>
  <verify>
    <automated>python3 -c "
import re
content = open('/Users/calling/perfectPixel_ver1.1/editor.html').read()
checks = [
  ('add-swatch' in content and 'addSwEl' in content, 'add-swatch injected in renderSwatches'),
  ('从本地上传' in content, 'upload option in refreshSavedDropdown'),
  ('已加载色卡' in content, 'load-on-select logic in refreshSavedDropdown'),
  ('paletteName' in content and 'paletteNameInputEl' in content, 'paletteName filled on select'),
]
failures = [msg for ok, msg in checks if not ok]
if failures:
    print('FAIL:', failures)
    exit(1)
print('ALL FUNCTION BODY CHECKS PASSED')
"
    </automated>
  </verify>
  <done>renderSwatches() 末尾追加 .add-swatch；refreshSavedDropdown() 点击条目即加载色卡；下拉底部有上传入口。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 4.2 完整的色卡面板 UI 整理：
    - 移除 paletteEnabled toggle（面板始终可用）
    - 线性布局：色卡区 → combobox → 生成区 → 固定底部（映射+应用）
    - + 色块：点击把前景色加入色卡
    - combobox：▼ 展开已保存列表，点击即加载；💾 保存；底部有「📁 从本地上传」
    - 导出▼：展开 .act/.gpl/.pal 三选项
    - 映射模式：`<select>` 下拉框（替代 radio card）
    - 底部固定区（映射 + 应用色卡）使用 flex-shrink:0，不随内容滚动消失
  </what-built>
  <how-to-verify>
    1. 确认 Flask 在 5010 端口运行：终端执行 `python3 web_app.py`（若未运行）
    2. 打开 http://localhost:5010/editor，加载任意图片
    3. 展开「色卡限制」面板（点击标题 chevron）：
       a. 确认顶部没有 toggle 开关
       b. 确认布局从上到下：当前色卡 → combobox → 生成区 → 固定底部
    4. 点击「生成色卡」生成几种颜色
    5. 检查色卡末尾有灰色「+」色块（.add-swatch）
    6. 在调色盘选一个颜色，点击「+」色块 → 确认该颜色追加到色卡末尾
    7. 点击「导出▼」→ 确认展开 .act / .gpl / .pal 三选项
    8. 点击 combobox 的「▼」→ 确认展开已保存列表（若有）；底部有「📁 从本地上传」
    9. 点击「📁 从本地上传」→ 确认弹出文件选择器
    10. 确认「映射」行显示为 `<select>` 下拉框（非 radio 按钮）
    11. 确认底部「映射 + 应用色卡」区域固定可见，不因面板内容过多而消失
    12. 打开浏览器 Console → 确认无 JS 报错（特别是 null 引用错误）
    13. 点击「应用色卡」→ 确认功能正常（使用 mappingModeSelect 值），画布颜色被替换
    14. Cmd+Z 撤销 → 确认撤销正常
  </how-to-verify>
  <resume-signal>输入 "approved" 表示验证通过，或描述发现的问题</resume-signal>
</task>

</tasks>

<verification>
所有自动化验证通过后，人工验证：
1. 面板布局符合设计文档（色卡区 → combobox → 生成区 → 固定底部）
2. + 色块、导出下拉、combobox 下拉、映射 select 均可交互
3. 应用色卡功能正常，可撤销
4. 无 JS console 错误
</verification>

<success_criteria>
- 所有 7 项决策（D1–D7）均在 editor.html 中实现
- 浏览器打开 /editor 无 console 错误
- 色卡面板所有交互功能正常（生成、加号添加、导出、combobox 加载、映射选择、应用、撤销）
- 底部固定区始终可见
- 人工验证 checkpoint 通过
</success_criteria>

<output>
完成后创建 `.planning/phases/04.2-palette-ui/04.2-02-SUMMARY.md`
</output>
