---
phase: 04.2-palette-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - editor.html
autonomous: true
requirements:
  - PAL-UI

must_haves:
  truths:
    - "面板顶部没有 toggle 开关，直接是色卡内容区"
    - "paletteBody 内部结构从上到下为：色卡区 → combobox 行 → 生成区 → 固定底部（映射+应用）"
    - "底部 .pal-sticky-bottom 使用 flex-shrink:0，不因内容滚动消失"
    - "swatchesGrid 后跟一个 .add-swatch 占位符（初始由 JS 渲染时注入，HTML 不含静态 swatch）"
    - "mappingModeSelect 是 <select> 元素，含 vector/perceptual/swap 三个 option"
    - "导出下拉按钮 #exportDropBtn 和 #exportMenu 存在于 .swatches-label 行内"
    - "combobox 行含 #paletteName input、#palDropBtn 按钮、#savePaletteBtn 按钮"
    - "#savedPaletteOptions 是 #palFileInput 的兄弟节点，父容器有 position:relative"
  artifacts:
    - path: "editor.html"
      provides: "重构后的 #paletteBody HTML 骨架 + 新增 CSS 类"
      contains: "pal-scroll-area, pal-sticky-bottom, add-swatch, combobox-row, export-dropdown, export-menu, mappingModeSelect"
  key_links:
    - from: ".pal-scroll-area"
      to: ".pal-sticky-bottom"
      via: "flex column in #paletteBody"
      pattern: "flex-shrink:0"
    - from: "#swatchesGrid"
      to: ".add-swatch"
      via: "renderSwatches() 末尾注入（Task 由 Plan 02 完成）"
      pattern: "add-swatch"
---

<objective>
替换 `editor.html` 中 `#paletteBody` 的全部 HTML 内容，并添加新增 CSS 类。

Purpose: 建立新 UI 骨架，使后续 Plan 02 的 JS 接线可以直接操作正确的 DOM ID。这是纯 HTML/CSS 改动，不涉及任何 JS 逻辑。
Output: editor.html 中 `#paletteBody` 内容从原有 tab/radio/多按钮结构替换为线性布局；添加 .pal-scroll-area / .pal-sticky-bottom / .add-swatch / .combobox-row / .export-dropdown / .export-menu / .mapping-row CSS 类定义。
</objective>

<execution_context>
@/Users/calling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/calling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-palette-ui/04.2-CONTEXT.md
@.planning/phases/04.2-palette-ui/04.2-RESEARCH.md

<interfaces>
<!-- 关键现有结构（来自 RESEARCH.md 行号分析） -->

现有 #paletteBody 范围（第 485–607 行）：
- 顶部 toggle switch: #paletteEnabled（第 477–480 行，位于 .palette-header，不在 paletteBody 内）
- .tabs + tab-btn×2（第 488–491 行）
- #tab-generate（第 494–521 行）— 含颜色数量 slider、算法 select、boost params、生成按钮
- #tab-upload（第 523–530 行）— 含 palUploadZone + palFileInput
- 当前色卡 block（第 532–544 行）— 含 swatchesGrid、swatchCount、addColorBtn
- 保存/加载 block（第 546–565 行）— 含 paletteName、savedPaletteTrigger、savePaletteBtn、loadPaletteBtn、deletePaletteBtn
- 映射模式 block（第 567–593 行）— 含 3 个 radio card
- 导出 block（第 595–603 行）— 含 exportAct/Gpl/Pal 三按钮
- 应用色卡 btn（第 605 行）— #applyPaletteBtn

现有 CSS（第 227 行）：
```css
.palette-body { padding: 12px; display: flex; flex-direction: column; gap: 10px; }
```

现有 palette-section（第 218 行）：
```css
.palette-section { overflow: hidden; ... }
```
注意：overflow:hidden 导致 position:sticky 在其内子元素失效 → 必须使用 flex-shrink:0 方案
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 替换 #paletteBody HTML 内容 + 删除 .palette-header 中的 toggle</name>
  <files>editor.html</files>
  <action>
在 editor.html 中执行以下修改：

**步骤 1：删除 .palette-header 中的 toggle switch**

定位 `#paletteEnabled` toggle（约第 477–480 行，在 `.palette-header` 内），形如：
```html
<label class="toggle-switch">
  <input type="checkbox" id="paletteEnabled" checked>
  <span class="toggle-slider"></span>
</label>
```
将整个 `<label class="toggle-switch">...</label>` 块删除。保留 `.palette-header` 中的标题文字（「色卡限制」）和 chevron（`#palChevron` 展开/收起按钮）不变。

**步骤 2：替换 #paletteBody 全部内容**

找到 `<div class="palette-body hidden" id="paletteBody">` 开始标签，到其对应的 `</div>` 结束标签（约第 485–607 行），将整个内部内容（开始和结束标签之间的所有内容）替换为以下新结构：

```html
  <!-- 可滚动主区 -->
  <div class="pal-scroll-area">

    <!-- 当前色卡 -->
    <div>
      <div class="swatches-label">
        <span class="section-label" style="margin:0;">当前色卡</span>
        <div style="display:flex;align-items:center;gap:6px;">
          <span class="swatches-count" id="swatchCount">0 色</span>
          <div class="export-dropdown" id="exportDropdown">
            <button class="btn btn-ghost" id="exportDropBtn" style="font-size:11px;padding:3px 7px;" title="导出色卡">导出▼</button>
            <div class="export-menu" id="exportMenu" style="display:none;">
              <button class="export-menu-item" id="exportAct">.act</button>
              <button class="export-menu-item" id="exportGpl">.gpl</button>
              <button class="export-menu-item" id="exportPal">.pal</button>
            </div>
          </div>
        </div>
      </div>
      <div style="font-size:11px;color:var(--text-muted);margin:3px 0 5px;">双击色块可更改颜色</div>
      <div class="swatches-outer">
        <div class="swatches-grid" id="swatchesGrid">
          <span class="swatches-empty">色卡为空</span>
        </div>
      </div>
    </div>

    <!-- Combobox：命名 / 选择 / 保存 -->
    <div style="position:relative;">
      <div class="combobox-row">
        <input type="text" id="paletteName" placeholder="未命名色卡">
        <button class="btn btn-ghost combobox-drop-btn" id="palDropBtn" title="选择/上传色卡">▼</button>
        <button class="btn btn-ghost" id="savePaletteBtn" title="保存色卡">💾</button>
      </div>
      <div class="custom-select-options" id="savedPaletteOptions" style="display:none; position:absolute; top:calc(100% + 4px); left:0; right:0; z-index:100;"></div>
      <input type="file" id="palFileInput" accept=".act,.gpl,.pal,.png" style="display:none;">
    </div>

    <!-- 生成区 -->
    <div style="display:flex;flex-direction:column;gap:8px;">
      <div class="control-group">
        <label>颜色数量</label>
        <div class="slider-row">
          <input type="range" id="nColors" min="2" max="64" step="1" value="16">
          <input type="number" id="nColorsInput" class="n-colors-input" value="16" min="1">
        </div>
      </div>
      <div class="control-group">
        <label>生成算法</label>
        <select id="genAlgorithm" style="padding:4px 6px; border-radius:5px; border:1px solid var(--border); background:var(--surface2); color:var(--text); font-size:12px;">
          <option value="fastoctree">快速八叉树（默认）</option>
          <option value="mediancut">中值切割</option>
          <option value="boost">覆盖增强 ★</option>
        </select>
      </div>
      <div id="boostParams" class="control-group" style="display:none;">
        <label>噪点过滤阈值</label>
        <div class="slider-row">
          <input type="range" id="minRegionPct" min="0.01" max="5" step="0.01" value="1.0">
          <input type="number" id="minRegionPctInput" class="n-colors-input" value="1.0" min="0.01" max="100" step="0.01">
        </div>
        <div style="font-size:11px;color:var(--text-muted);margin-top:2px;">仅保留面积 ≥ X% 的连通色块区域</div>
      </div>
      <button class="btn btn-secondary" id="generateBtn" style="width:100%;">生成色卡</button>
    </div>

  </div><!-- /pal-scroll-area -->

  <!-- 固定底部：映射模式 + 应用色卡 -->
  <div class="pal-sticky-bottom">
    <div class="mapping-row">
      <span class="section-label" style="margin:0; white-space:nowrap;">映射</span>
      <select id="mappingModeSelect" style="flex:1; padding:4px 6px; border-radius:5px; border:1px solid var(--border); background:var(--surface2); color:var(--text); font-size:12px;">
        <option value="vector">向量匹配（RGB 欧氏距离）</option>
        <option value="perceptual">感知匹配（LAB 色彩空间）</option>
        <option value="swap">色卡替换（量化防扎堆）</option>
      </select>
    </div>
    <button class="btn btn-primary" id="applyPaletteBtn" disabled style="width:100%;">应用色卡</button>
  </div>
```

注意：`<div class="palette-body hidden" id="paletteBody">` 和 `</div>` 开关标签本身保留不变，只替换内部内容。

**步骤 3：在 `<style>` 中添加新 CSS 类**

找到 `.palette-body {` CSS 规则（约第 227 行），将其改为：

```css
.palette-body {
  display: flex;
  flex-direction: column;
  overflow: hidden;
  max-height: 50vh;
}
.pal-scroll-area {
  flex: 1;
  overflow-y: auto;
  padding: 12px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.pal-sticky-bottom {
  flex-shrink: 0;
  padding: 10px 12px;
  border-top: 1px solid var(--border);
  background: var(--surface);
  display: flex;
  flex-direction: column;
  gap: 8px;
}
```

在 `.add-swatch` CSS 类不存在时新增（在 `.palette-swatch` 或 `.swatches-grid` 附近添加）：

```css
.add-swatch {
  width: 28px; height: 28px; border-radius: 4px; cursor: pointer;
  border: 2px dashed #666; background: #555;
  display: flex; align-items: center; justify-content: center;
  color: #ccc; font-size: 18px; font-weight: 300; line-height: 1;
  flex-shrink: 0; transition: border-color .1s, background .1s;
  user-select: none;
}
.add-swatch:hover { border-color: var(--accent); background: #666; color: #fff; }
```

在适当位置添加 combobox 和导出下拉 CSS（在 `.palette-body` 附近）：

```css
.combobox-row {
  display: flex; gap: 4px; align-items: center;
}
.combobox-row input[type=text] {
  flex: 1; padding: 5px 8px; border-radius: 6px;
  border: 1px solid var(--border); background: var(--surface2);
  color: var(--text); font-size: 13px; min-width: 0;
}
.combobox-row input[type=text]:focus { border-color: var(--accent); outline: none; }
.combobox-drop-btn { padding: 5px 8px; font-size: 10px; }
.export-dropdown { position: relative; }
.export-menu {
  position: absolute; right: 0; top: calc(100% + 4px);
  background: var(--surface); border: 1px solid var(--border);
  border-radius: 6px; z-index: 200; min-width: 80px;
  box-shadow: 0 4px 16px rgba(0,0,0,.4);
}
.export-menu-item {
  display: block; width: 100%; padding: 7px 12px; background: transparent;
  border: none; color: var(--text); font-size: 12px; cursor: pointer;
  text-align: left; border-bottom: 1px solid var(--border);
}
.export-menu-item:last-child { border-bottom: none; }
.export-menu-item:hover { background: rgba(124,106,247,.1); }
.mapping-row {
  display: flex; align-items: center; gap: 8px;
}
```

**不要做的事（关键约束）：**
- 不要用 position:sticky——.palette-section 有 overflow:hidden，sticky 会失效
- 不要修改 .swatches-label 现有 CSS（若已有则复用，仅确保 display:flex; justify-content:space-between）
- 不要在 HTML 中静态放置 .add-swatch——它由 Plan 02 的 renderSwatches() 动态注入
- 不要删除或修改任何 JS 代码（本任务仅 HTML + CSS）
- 不要修改 .palette-header（除删除 toggle 外），保留 chevron 展开/收起功能
  </action>
  <verify>
    <automated>python3 -c "
content = open('/Users/calling/perfectPixel_ver1.1/editor.html').read()
checks = [
  ('pal-scroll-area' in content, 'pal-scroll-area class exists'),
  ('pal-sticky-bottom' in content, 'pal-sticky-bottom class exists'),
  ('add-swatch' in content, 'add-swatch class exists'),
  ('combobox-row' in content, 'combobox-row class exists'),
  ('palDropBtn' in content, 'palDropBtn exists'),
  ('paletteName' in content, 'paletteName input exists'),
  ('mappingModeSelect' in content, 'mappingModeSelect exists'),
  ('exportDropBtn' in content, 'exportDropBtn exists'),
  ('exportMenu' in content, 'exportMenu exists'),
  ('savedPaletteOptions' in content, 'savedPaletteOptions dropdown exists'),
  ('paletteEnabled' not in content, 'paletteEnabled toggle removed'),
  ('tab-generate' not in content, 'tab-generate removed'),
  ('tab-upload' not in content, 'tab-upload removed'),
  ('mode-group' not in content, 'radio card mode-group removed'),
  ('addColorBtn' not in content, 'addColorBtn removed'),
]
failures = [msg for ok, msg in checks if not ok]
if failures:
    print('FAIL:', failures)
    exit(1)
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <done>editor.html 的 #paletteBody 内容已替换为新骨架；新 CSS 类已定义；paletteEnabled toggle 已删除；验证脚本全部通过。</done>
</task>

</tasks>

<verification>
运行验证脚本确认所有新元素存在、所有旧元素已删除：
`python3 -c "content = open('editor.html').read(); print('OK' if 'pal-scroll-area' in content and 'mappingModeSelect' in content and 'paletteEnabled' not in content else 'FAIL')"`
</verification>

<success_criteria>
- editor.html 加载不报 JS 错误（Flask 运行时打开 http://localhost:5010/editor 无 console error）
- #paletteBody 内部结构符合设计文档（色卡区 → combobox → 生成区 → 固定底部）
- 所有新 DOM id（palDropBtn, paletteName, savePaletteBtn, savedPaletteOptions, mappingModeSelect, exportDropBtn, exportMenu）存在于 HTML 中
- 所有旧元素（paletteEnabled, tab-generate, tab-upload, mode-group, addColorBtn）不再出现
</success_criteria>

<output>
完成后创建 `.planning/phases/04.2-palette-ui/04.2-01-SUMMARY.md`
</output>
